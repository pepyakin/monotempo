# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_binary", "crate_cargo_toml_env_vars", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_binary(
    name = "example-custom-payload-builder",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    deps = [
        "//alloy/crates/eips:alloy_eips",
        "//reth/crates/ethereum/payload:reth_ethereum_payload_builder",
        "//reth/crates/ethereum/reth:reth_ethereum",
        "//reth/crates/payload/basic:reth_basic_payload_builder",
        "//reth/crates/payload/builder:reth_payload_builder",
        "@crates//:eyre",
        "@crates//:futures-util",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "example_custom_payload_builder_test",
    workspace = WORKSPACE,
    crate = ":example-custom-payload-builder",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
)
