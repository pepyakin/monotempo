# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 scripts/bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "reth_binary", "reth_cargo_toml_env_vars", "reth_unit_test")

FEATURES = []

DECLARED_FEATURES = []

reth_cargo_toml_env_vars()

reth_binary(
    name = "custom-hardforks",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    deps = [
        "//crates/chainspec:reth_chainspec",
        "//crates/net/peers:reth_network_peers",
        "@crates//:alloy-consensus",
        "@crates//:alloy-eips",
        "@crates//:alloy-genesis",
        "@crates//:alloy-primitives",
        "@crates//:serde",
    ],
)

reth_unit_test(
    name = "custom_hardforks_test",
    crate = ":custom-hardforks",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
)
