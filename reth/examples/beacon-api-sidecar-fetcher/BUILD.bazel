# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_binary", "crate_cargo_toml_env_vars", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_binary(
    name = "example-beacon-api-sidecar-fetcher",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    deps = [
        "//alloy/crates/consensus:alloy_consensus",
        "//alloy/crates/eips:alloy_eips",
        "//alloy/crates/rpc-types-beacon:alloy_rpc_types_beacon",
        "//reth/crates/ethereum/reth:reth_ethereum",
        "@crates//:alloy-primitives",
        "@crates//:clap",
        "@crates//:eyre",
        "@crates//:futures-util",
        "@crates//:reqwest",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:thiserror",
    ],
)

crate_unit_test(
    name = "example_beacon_api_sidecar_fetcher_test",
    workspace = WORKSPACE,
    crate = ":example-beacon-api-sidecar-fetcher",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
)
