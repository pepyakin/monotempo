# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "pin-project",
    "rayon",
    "test-utils",
]

DECLARED_FEATURES = [
    "pin-project",
    "rayon",
    "test-utils",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_tasks",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/metrics:reth_metrics",
        "@reth_crates//:crossbeam-utils",
        "@reth_crates//:dashmap",
        "@reth_crates//:futures-util",
        "@reth_crates//:metrics",
        "@reth_crates//:parking_lot",
        "@reth_crates//:pin-project",
        "@reth_crates//:rayon",
        "@reth_crates//:thiserror",
        "@reth_crates//:thread-priority",
        "@reth_crates//:tokio",
        "@reth_crates//:tracing",
        "@reth_crates//:tracing-futures",
    ] + select({
        "@platforms//os:linux": ["@reth_crates//:libc"],
        "//conditions:default": [],
    }),
)

crate_unit_test(
    name = "reth_tasks_test",
    workspace = WORKSPACE,
    crate = ":reth_tasks",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
