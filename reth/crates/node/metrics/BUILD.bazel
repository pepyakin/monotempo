# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "jemalloc",
]

DECLARED_FEATURES = [
    "jemalloc",
    "jemalloc-prof",
    "jemalloc-symbols",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_node_metrics",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/metrics:reth_metrics",
        "//reth/crates/tasks:reth_tasks",
        "@crates//:bytes",
        "@crates//:eyre",
        "@crates//:http",
        "@crates//:http-body-util",
        "@crates//:jsonrpsee-server",
        "@crates//:metrics",
        "@crates//:metrics-exporter-prometheus",
        "@crates//:metrics-process",
        "@crates//:metrics-util",
        "@crates//:parking_lot",
        "@crates//:reqwest",
        "@crates//:tokio",
        "@crates//:tower",
        "@crates//:tracing",
    ] + select({
        "@platforms//os:linux": ["@crates//:procfs"],
        "//conditions:default": [],
    }) + select({
        "@platforms//os:linux": ["@crates//:tikv-jemalloc-ctl"],
        "@platforms//os:macos": ["@crates//:tikv-jemalloc-ctl"],
        "//conditions:default": [],
    }),
)

crate_unit_test(
    name = "reth_node_metrics_test",
    workspace = WORKSPACE,
    crate = ":reth_node_metrics",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:socket2",
    ],
)
