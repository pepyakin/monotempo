# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "jemalloc",
]

DECLARED_FEATURES = [
    "jemalloc",
    "jemalloc-prof",
    "jemalloc-symbols",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_node_metrics",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/metrics:reth_metrics",
        "//reth/crates/tasks:reth_tasks",
        "@reth_crates//:bytes",
        "@reth_crates//:eyre",
        "@reth_crates//:http",
        "@reth_crates//:http-body-util",
        "@reth_crates//:jsonrpsee-server",
        "@reth_crates//:metrics",
        "@reth_crates//:metrics-exporter-prometheus",
        "@reth_crates//:metrics-process",
        "@reth_crates//:metrics-util",
        "@reth_crates//:parking_lot",
        "@reth_crates//:reqwest",
        "@reth_crates//:tokio",
        "@reth_crates//:tower",
        "@reth_crates//:tracing",
    ] + select({
        "@platforms//os:linux": ["@reth_crates//:procfs"],
        "//conditions:default": [],
    }) + select({
        "@platforms//os:linux": ["@reth_crates//:tikv-jemalloc-ctl"],
        "@platforms//os:macos": ["@reth_crates//:tikv-jemalloc-ctl"],
        "//conditions:default": [],
    }),
)

crate_unit_test(
    name = "reth_node_metrics_test",
    workspace = WORKSPACE,
    crate = ":reth_node_metrics",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@reth_crates//:socket2",
    ],
)
