# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "clap",
    "default",
    "std",
]

DECLARED_FEATURES = [
    "clap",
    "default",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_static_file_types",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/stages/types:reth_stages_types",
        "@crates//:alloy-primitives",
        "@crates//:clap",
        "@crates//:derive_more",
        "@crates//:fixed-map",
        "@crates//:serde",
        "@crates//:strum",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "reth_static_file_types_test",
    workspace = WORKSPACE,
    crate = ":reth_static_file_types",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/storage/nippy-jar:reth_nippy_jar",
        "@crates//:insta",
        "@crates//:serde_json",
        "@crates//:tempfile",
    ],
)
