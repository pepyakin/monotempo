# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 reth/scripts/bazel/generate.py` after changing Cargo.toml.

load("//reth/bazel:rust.bzl", "reth_cargo_toml_env_vars", "reth_library", "reth_unit_test")

FEATURES = [
    "jemalloc",
]

DECLARED_FEATURES = [
    "jemalloc",
    "jemalloc-prof",
    "snmalloc",
    "snmalloc-native",
    "tracy-allocator",
]

reth_cargo_toml_env_vars()

reth_library(
    name = "reth_cli_util",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth/crates/fs-util:reth_fs_util",
        "@reth_crates//:alloy-eips",
        "@reth_crates//:alloy-primitives",
        "@reth_crates//:cfg-if",
        "@reth_crates//:eyre",
        "@reth_crates//:rand_08-0.8.6",
        "@reth_crates//:secp256k1",
        "@reth_crates//:serde",
        "@reth_crates//:thiserror",
    ] + select({
        "@platforms//os:linux": ["@reth_crates//:libc", "@reth_crates//:tikv-jemalloc-sys", "@reth_crates//:tikv-jemallocator"],
        "@platforms//os:macos": ["@reth_crates//:libc", "@reth_crates//:tikv-jemalloc-sys", "@reth_crates//:tikv-jemallocator"],
        "//conditions:default": [],
    }),
    aliases = {
        "@reth_crates//:rand_08-0.8.6": "rand_08",
    },
)

reth_unit_test(
    name = "reth_cli_util_test",
    crate = ":reth_cli_util",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@reth_crates//:rand-0.9.4",
    ],
)
