# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 scripts/bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "reth_build_script", "reth_cargo_toml_env_vars", "reth_library", "reth_unit_test")

FEATURES = []

DECLARED_FEATURES = []

reth_cargo_toml_env_vars()

reth_build_script(
    name = "reth_mdbx_sys_build_script",
    crate_features = FEATURES,
    deps = [
        "@crates//:bindgen",
        "@crates//:cc",
    ],
    data = glob(["libmdbx/**"]) + [
        "//bazel:clang_builtin_headers",
        "@llvm_toolchain_llvm//:libclang",
    ],
    build_script_env = {
        "LIBCLANG_PATH": "$(execpath @llvm_toolchain_llvm//:libclang)",
        "BINDGEN_EXTRA_CLANG_ARGS": "-resource-dir=$(execpath //bazel:clang_builtin_headers)/..",
    },
)

reth_library(
    name = "reth_mdbx_sys",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    build_script = ":reth_mdbx_sys_build_script",
)

reth_unit_test(
    name = "reth_mdbx_sys_test",
    crate = ":reth_mdbx_sys",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
)
