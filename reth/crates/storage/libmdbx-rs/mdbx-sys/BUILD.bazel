# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_build_script", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_build_script(
    name = "reth_mdbx_sys_build_script",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    deps = [
        "@crates//:bindgen",
        "@crates//:cc",
    ],
    data = glob([
        "libmdbx/**",
    ]) + [
        "//bazel:clang_builtin_headers",
        "@llvm_toolchain_llvm//:libclang",
    ],
    build_script_env = {
        "LIBCLANG_PATH": "$(execpath @llvm_toolchain_llvm//:libclang)",
        "BINDGEN_EXTRA_CLANG_ARGS": "-resource-dir=$(execpath //bazel:clang_builtin_headers)/..",
    },
)

crate_library(
    name = "reth_mdbx_sys",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    build_script = ":reth_mdbx_sys_build_script",
)

crate_unit_test(
    name = "reth_mdbx_sys_test",
    workspace = WORKSPACE,
    crate = ":reth_mdbx_sys",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
)
