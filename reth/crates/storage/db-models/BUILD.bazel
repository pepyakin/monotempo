# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "arbitrary",
    "default",
    "reth-codec",
    "serde",
    "std",
    "test-utils",
]

DECLARED_FEATURES = [
    "arbitrary",
    "default",
    "reth-codec",
    "serde",
    "std",
    "test-utils",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_db_models",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/eips:alloy_eips",
        "//reth-core/crates/codecs:reth_codecs",
        "//reth-core/crates/primitives-traits:reth_primitives_traits",
        "@crates//:alloy-primitives",
        "@crates//:arbitrary",
        "@crates//:bytes",
        "@crates//:modular-bitfield",
        "@crates//:serde",
    ],
)

crate_unit_test(
    name = "reth_db_models_test",
    workspace = WORKSPACE,
    crate = ":reth_db_models",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:proptest",
        "@crates//:proptest-arbitrary-interop",
    ],
)
