# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "arbitrary",
    "default",
    "rustc-hash",
    "serde",
    "std",
]

DECLARED_FEATURES = [
    "arbitrary",
    "default",
    "rustc-hash",
    "serde",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_ethereum_forks",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:alloy-eip2124",
        "@crates//:alloy-hardforks-0.4.8",
        "@crates//:alloy-primitives",
        "@crates//:arbitrary",
        "@crates//:once_cell",
        "@crates//:rustc-hash",
    ],
    proc_macro_deps = [
        "@crates//:auto_impl",
    ],
)

crate_unit_test(
    name = "reth_ethereum_forks_test",
    workspace = WORKSPACE,
    crate = ":reth_ethereum_forks",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
