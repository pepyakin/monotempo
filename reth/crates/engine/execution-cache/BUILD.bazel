# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "test-utils",
]

DECLARED_FEATURES = [
    "test-utils",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_execution_cache",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//reth-core/crates/primitives-traits:reth_primitives_traits",
        "//reth/crates/errors:reth_errors",
        "//reth/crates/metrics:reth_metrics",
        "//reth/crates/revm:reth_revm",
        "//reth/crates/storage/provider:reth_provider",
        "//reth/crates/trie/trie:reth_trie",
        "@crates//:alloy-primitives",
        "@crates//:fixed-cache",
        "@crates//:metrics",
        "@crates//:parking_lot",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "reth_execution_cache_test",
    workspace = WORKSPACE,
    crate = ":reth_execution_cache",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:revm",
    ],
)
