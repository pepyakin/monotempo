# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 scripts/bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "reth_cargo_toml_env_vars", "reth_library", "reth_unit_test")

FEATURES = [
    "serde",
]

DECLARED_FEATURES = [
    "serde",
]

reth_cargo_toml_env_vars()

reth_library(
    name = "reth_config",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//crates/net/network-types:reth_network_types",
        "//crates/net/peers:reth_network_peers",
        "//crates/prune/types:reth_prune_types",
        "//crates/stages/types:reth_stages_types",
        "//crates/static-file/types:reth_static_file_types",
        "@crates//:eyre",
        "@crates//:humantime-serde",
        "@crates//:serde",
        "@crates//:toml",
        "@crates//:url",
    ],
)

reth_unit_test(
    name = "reth_config_test",
    crate = ":reth_config",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:alloy-primitives",
        "@crates//:tempfile",
    ],
)
