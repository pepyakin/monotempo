# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "std",
]

DECLARED_FEATURES = [
    "default",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_execution_errors",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-evm/crates/evm:alloy_evm",
        "//reth/crates/storage/errors:reth_storage_errors",
        "@crates//:alloy-primitives",
        "@crates//:alloy-rlp",
        "@crates//:nybbles",
        "@crates//:thiserror",
    ],
)

crate_unit_test(
    name = "reth_execution_errors_test",
    workspace = WORKSPACE,
    crate = ":reth_execution_errors",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
