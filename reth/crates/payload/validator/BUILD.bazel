# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "std",
]

DECLARED_FEATURES = [
    "default",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_payload_validator",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/consensus:alloy_consensus",
        "//alloy/crates/rpc-types-engine:alloy_rpc_types_engine",
        "@crates//:reth-primitives-traits",
    ],
)

crate_unit_test(
    name = "reth_payload_validator_test",
    workspace = WORKSPACE,
    crate = ":reth_payload_validator",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
