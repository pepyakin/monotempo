# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//reth/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "reth_rpc_layer",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/rpc-types-engine:alloy_rpc_types_engine",
        "@crates//:http",
        "@crates//:http-body-util",
        "@crates//:jsonrpsee-http-client",
        "@crates//:pin-project",
        "@crates//:tokio",
        "@crates//:tower",
        "@crates//:tower-http",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "reth_rpc_layer_test",
    workspace = WORKSPACE,
    crate = ":reth_rpc_layer",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:brotli",
        "@crates//:flate2",
        "@crates//:jsonrpsee",
        "@crates//:reqwest",
        "@crates//:zstd",
    ],
)
