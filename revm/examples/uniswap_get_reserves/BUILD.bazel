# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_binary", "crate_cargo_toml_env_vars", "crate_unit_test")
load("//revm/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_binary(
    name = "example-uniswap-get-reserves",
    workspace = WORKSPACE,
    version = "0.0.0",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/sol-types:alloy_sol_types",
        "//alloy/crates/eips:alloy_eips",
        "//alloy/crates/provider:alloy_provider",
        "//revm/crates/revm:revm",
        "@crates//:anyhow",
        "@crates//:tokio",
    ],
)

crate_unit_test(
    name = "example_uniswap_get_reserves_test",
    workspace = WORKSPACE,
    version = "0.0.0",
    crate = ":example-uniswap-get-reserves",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
