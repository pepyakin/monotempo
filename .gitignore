# Bazel convenience symlinks and per-machine overrides.
/bazel-*
/user.bazelrc

# Amp's per-orb portal metadata.
/.amp/portals/*

# Cargo, when used directly inside a project.
target/

.idea
.vscode
.DS_Store
