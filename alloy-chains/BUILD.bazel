# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//alloy-chains/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "arbitrary",
    "default",
    "rlp",
    "serde",
    "std",
]

DECLARED_FEATURES = [
    "arbitrary",
    "default",
    "rlp",
    "schema",
    "serde",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_chains",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy-rlp/crates/rlp:alloy_rlp",
        "@crates//:arbitrary",
        "@crates//:num_enum",
        "@crates//:phf",
        "@crates//:proptest",
        "@crates//:serde",
    ],
)

crate_unit_test(
    name = "alloy_chains_test",
    workspace = WORKSPACE,
    crate = ":alloy_chains",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    workspace_lints = False,
    deps = [
        "@crates//:serde_json",
    ],
)
