# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/node/metrics/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-node-metrics"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_node_metrics"
path = "src/lib.rs"

[features]
jemalloc = ["dep:tikv-jemalloc-ctl"]
jemalloc-prof = [
    "jemalloc",
    "dep:jemalloc_pprof",
    "dep:mappings",
    "dep:pprof_util",
    "dep:reth-fs-util",
    "dep:tempfile",
]
jemalloc-symbols = ["jemalloc-prof", "jemalloc_pprof?/symbolize"]

[dependencies]
bytes = { version = "^1.11.1", default-features = false }
eyre = { version = "^0.6" }
http = { version = "^1.0" }
http-body-util = { version = "^0.1.2" }
jsonrpsee-server = { version = "^0.26.0" }
metrics = { version = "^0.24.0" }
metrics-exporter-prometheus = { version = "^0.18.3", default-features = false }
metrics-process = { version = "^2.1.0" }
metrics-util = { version = "^0.20.3", default-features = false }
parking_lot = { version = "^0.12" }
reqwest = { version = "^0.13", features = ["rustls", "stream"], default-features = false }
reth-metrics = { path = "../../metrics" }
reth-tasks = { path = "../../tasks" }
tokio = { version = "^1.52.3", default-features = false }
tower = { version = "^0.5" }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
socket2 = { version = "^0.6", default-features = false }

[target.'cfg(target_os = "linux")'.dependencies]
procfs = { version = "^0.18.0" }

[target.'cfg(unix)'.dependencies]
jemalloc_pprof = { version = "^0.8", optional = true, default-features = false }
mappings = { version = "^0.7", optional = true }
pprof_util = { version = "^0.8", optional = true }
reth-fs-util = { path = "../../fs-util", optional = true }
tempfile = { version = "^3.20", optional = true }
tikv-jemalloc-ctl = { version = "^0.6", features = ["stats"], optional = true }
