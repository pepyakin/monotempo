# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/node/ethstats/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-node-ethstats"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_node_ethstats"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
chrono = { version = "^0.4.41" }
futures-util = { version = "^0.3", default-features = false }
reth-chain-state = { path = "../../chain-state" }
reth-network-api = { path = "../../net/network-api" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-storage-api = { path = "../../storage/storage-api", default-features = false }
reth-transaction-pool = { path = "../../transaction-pool" }
serde = { version = "^1.0", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
thiserror = { version = "^2.0.0", features = ["std"], default-features = false }
tokio = { version = "^1.52.3", default-features = false }
tokio-stream = { version = "^0.1.11" }
tokio-tungstenite = { version = "^0.29.0", features = ["rustls-tls-native-roots"] }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
url = { version = "^2.3", default-features = false }
