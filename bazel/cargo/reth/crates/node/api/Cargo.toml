# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/node/api/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-node-api"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_node_api"
path = "src/lib.rs"

[dependencies]
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
eyre = { version = "^0.6" }
reth-basic-payload-builder = { path = "../../payload/basic" }
reth-consensus = { path = "../../consensus/consensus", default-features = false }
reth-db-api = { path = "../../storage/db-api" }
reth-engine-primitives = { path = "../../engine/primitives", default-features = false }
reth-evm = { path = "../../evm/evm", default-features = false }
reth-network-api = { path = "../../net/network-api" }
reth-node-core = { path = "../core" }
reth-node-types = { path = "../types" }
reth-payload-builder = { path = "../../payload/builder" }
reth-payload-builder-primitives = { path = "../../payload/builder-primitives" }
reth-payload-primitives = { path = "../../payload/primitives" }
reth-provider = { path = "../../storage/provider" }
reth-tasks = { path = "../../tasks" }
reth-tokio-util = { path = "../../tokio-util" }
reth-transaction-pool = { path = "../../transaction-pool" }
