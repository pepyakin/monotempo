# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/revm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-revm"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_revm"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "reth-primitives-traits/std",
    "alloy-primitives/std",
    "alloy-rlp?/std",
    "revm/std",
    "alloy-consensus/std",
    "reth-ethereum-forks/std",
    "reth-storage-api/std",
    "reth-storage-errors/std",
]
witness = ["dep:reth-trie", "dep:alloy-rlp", "dep:alloy-rpc-types-debug"]
test-utils = ["dep:reth-trie", "reth-primitives-traits/test-utils", "reth-trie?/test-utils"]
serde = [
    "revm/serde",
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "reth-trie?/serde",
    "reth-ethereum-forks/serde",
    "reth-primitives-traits/serde",
    "reth-storage-api/serde",
]
portable = ["revm/portable"]
gmp = ["revm/gmp"]
optional-balance-check = ["revm/optional_balance_check"]
optional-block-gas-limit = ["revm/optional_block_gas_limit"]
optional-eip3541 = ["revm/optional_eip3541"]
optional-eip3607 = ["revm/optional_eip3607"]
optional-no-base-fee = ["revm/optional_no_base_fee"]
optional-checks = [
    "optional-balance-check",
    "optional-block-gas-limit",
    "optional-eip3541",
    "optional-eip3607",
    "optional-no-base-fee",
]
memory_limit = ["revm/memory_limit"]

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], optional = true, default-features = false }
alloy-rpc-types-debug = { version = "^2.4.2", optional = true, default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-storage-api = { path = "../storage/storage-api", default-features = false }
reth-storage-errors = { path = "../storage/errors", default-features = false }
reth-trie = { path = "../trie/trie", optional = true }
revm = { version = "^43.0.2", default-features = false }

[dev-dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
reth-ethereum-forks = { path = "../ethereum/hardforks", default-features = false }
reth-trie = { path = "../trie/trie" }
