# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/rpc/rpc-server-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-rpc-server-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_rpc_server_types"
path = "src/lib.rs"

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
jsonrpsee-core = { version = "^0.26.0" }
jsonrpsee-types = { version = "^0.26.0" }
reth-errors = { path = "../../errors" }
reth-network-api = { path = "../../net/network-api" }
serde = { version = "^1.0", features = ["derive"], default-features = false }
strum = { version = "^0.27", features = ["derive"], default-features = false }
