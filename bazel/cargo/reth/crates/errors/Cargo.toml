# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/errors/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-errors"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_errors"
path = "src/lib.rs"

[dependencies]
reth-consensus = { path = "../consensus/consensus", default-features = false }
reth-execution-errors = { path = "../evm/execution-errors", default-features = false }
reth-storage-errors = { path = "../storage/errors", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
