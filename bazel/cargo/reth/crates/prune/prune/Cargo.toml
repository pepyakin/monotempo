# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/prune/prune/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-prune"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_prune"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
itertools = { version = "^0.14", default-features = false }
metrics = { version = "^0.24.0" }
reth-config = { path = "../../config", default-features = false }
reth-db-api = { path = "../../storage/db-api" }
reth-errors = { path = "../../errors" }
reth-exex-types = { path = "../../exex/types" }
reth-metrics = { path = "../../metrics" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../../storage/provider" }
reth-prune-types = { path = "../types", default-features = false }
reth-stages-types = { path = "../../stages/types", default-features = false }
reth-static-file-types = { path = "../../static-file/types", default-features = false }
reth-storage-api = { path = "../../storage/storage-api", default-features = false }
reth-tokio-util = { path = "../../tokio-util" }
rustc-hash = { version = "^2.0", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
assert_matches = { version = "^1.5.0" }
reth-db = { path = "../../storage/db", features = ["test-utils"], default-features = false }
reth-primitives-traits = { version = "^0.7.1", features = ["arbitrary"], default-features = false }
reth-stages = { path = "../../stages/stages", features = ["test-utils"] }
reth-testing-utils = { path = "../../../testing/testing-utils" }
reth-tracing = { path = "../../tracing" }
