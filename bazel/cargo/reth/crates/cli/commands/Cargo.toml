# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/cli/commands/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-cli-commands"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_cli_commands"
path = "src/lib.rs"

[features]
arbitrary = [
    "dep:proptest",
    "dep:arbitrary",
    "dep:proptest-arbitrary-interop",
    "dep:reth-trie-common",
    "reth-db-api/arbitrary",
    "reth-eth-wire/arbitrary",
    "reth-db/arbitrary",
    "reth-chainspec/arbitrary",
    "alloy-chains/arbitrary",
    "alloy-eip7928/arbitrary",
    "alloy-eips/arbitrary",
    "alloy-primitives/arbitrary",
    "reth-codecs/test-utils",
    "reth-prune-types/test-utils",
    "reth-stages-types/test-utils",
    "reth-trie-common?/test-utils",
    "reth-codecs/arbitrary",
    "reth-prune-types/arbitrary",
    "reth-stages-types/arbitrary",
    "reth-trie-common?/arbitrary",
    "alloy-consensus/arbitrary",
    "reth-primitives-traits/arbitrary",
    "reth-ethereum-primitives/arbitrary",
]

[dependencies]
alloy-chains = { version = "^0.2.33", default-features = false }
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp"], default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
arbitrary = { version = "^1.3", optional = true }
backon = { version = "^1.2", features = ["std-blocking-sleep", "tokio-sleep"], default-features = false }
blake3 = { version = "^1.8" }
clap = { version = "^4", features = ["derive", "env"] }
comfy-table = { version = "^7.0" }
crossterm = { version = "^0.29.0" }
eyre = { version = "^0.6" }
fdlimit = { version = "^0.3.0" }
futures = { version = "^0.3" }
human_bytes = { version = "^0.4.1" }
humantime = { version = "^2.1" }
itertools = { version = "^0.14", default-features = false }
lz4 = { version = "^1.28.1" }
metrics = { version = "^0.24.0" }
parking_lot = { version = "^0.12" }
proptest = { version = "^1.7", optional = true }
proptest-arbitrary-interop = { version = "^0.1.0", optional = true }
ratatui = { version = "^0.30", features = ["crossterm"], default-features = false }
rayon = { version = "^1.7" }
reqwest = { version = "^0.13", features = ["rustls", "stream", "blocking"], default-features = false }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-cli = { path = "../cli" }
reth-cli-runner = { path = "../runner" }
reth-cli-util = { path = "../util" }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-config = { path = "../../config", features = ["serde"], default-features = false }
reth-consensus = { path = "../../consensus/consensus", default-features = false }
reth-db = { path = "../../storage/db", features = ["mdbx"], default-features = false }
reth-db-api = { path = "../../storage/db-api" }
reth-db-common = { path = "../../storage/db-common" }
reth-discv4 = { path = "../../net/discv4" }
reth-discv5 = { path = "../../net/discv5" }
reth-downloaders = { path = "../../net/downloaders", features = ["file-client"] }
reth-ecies = { path = "../../net/ecies" }
reth-era = { path = "../../era" }
reth-era-downloader = { path = "../../era-downloader" }
reth-era-utils = { path = "../../era-utils" }
reth-eth-wire = { path = "../../net/eth-wire" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", optional = true, default-features = false }
reth-etl = { path = "../../etl" }
reth-evm = { path = "../../evm/evm", default-features = false }
reth-exex = { path = "../../exex/exex" }
reth-fs-util = { path = "../../fs-util" }
reth-net-nat = { path = "../../net/nat" }
reth-network = { path = "../../net/network", features = ["serde"] }
reth-network-p2p = { path = "../../net/p2p" }
reth-network-peers = { path = "../../net/peers", features = ["secp256k1"], default-features = false }
reth-node-api = { path = "../../node/api" }
reth-node-builder = { path = "../../node/builder" }
reth-node-core = { path = "../../node/core" }
reth-node-events = { path = "../../node/events" }
reth-node-metrics = { path = "../../node/metrics" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../../storage/provider" }
reth-prune = { path = "../../prune/prune" }
reth-prune-types = { path = "../../prune/types", default-features = false }
reth-revm = { path = "../../revm", default-features = false }
reth-stages = { path = "../../stages/stages" }
reth-stages-types = { path = "../../stages/types", default-features = false }
reth-static-file = { path = "../../static-file/static-file" }
reth-static-file-types = { path = "../../static-file/types", features = ["clap"], default-features = false }
reth-storage-api = { path = "../../storage/storage-api", default-features = false }
reth-tasks = { path = "../../tasks" }
reth-trie = { path = "../../trie/trie", features = ["metrics"] }
reth-trie-common = { path = "../../trie/common", optional = true, default-features = false }
reth-trie-db = { path = "../../trie/db", features = ["metrics"] }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "global-context", "std", "recovery"], default-features = false }
serde = { version = "^1.0", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
socket2 = { version = "^0.6", features = ["all"], default-features = false }
tar = { version = "^0.4.46" }
tokio = { version = "^1.52.3", features = ["net"], default-features = false }
tokio-stream = { version = "^0.1.11" }
toml = { version = "^0.9", features = ["display"] }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
url = { version = "^2.3", default-features = false }
zstd = { version = "^0.13" }

[dev-dependencies]
reth-ethereum-cli = { path = "../../ethereum/cli", default-features = false }
reth-provider = { path = "../../storage/provider", features = ["test-utils"] }
tempfile = { version = "^3.20" }
tokio = { version = "^1.52.3", features = ["macros", "rt-multi-thread"], default-features = false }
