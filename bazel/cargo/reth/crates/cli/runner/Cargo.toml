# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/cli/runner/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-cli-runner"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_cli_runner"
path = "src/lib.rs"

[dependencies]
reth-tasks = { path = "../../tasks" }
tokio = { version = "^1.52.3", features = ["macros", "rt-multi-thread", "signal"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
