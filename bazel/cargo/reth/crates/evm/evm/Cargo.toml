# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/evm/evm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-evm"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_evm"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "dep:rayon",
    "reth-primitives-traits/std",
    "alloy-eips/std",
    "alloy-primitives/std",
    "alloy-consensus/std",
    "revm/std",
    "alloy-evm/std",
    "reth-execution-errors/std",
    "reth-execution-types/std",
    "reth-storage-errors/std",
    "futures-util/std",
    "derive_more/std",
    "reth-storage-api/std",
    "reth-trie-common/std",
    "reth-ethereum-primitives/std",
    "alloy-eip7928/std",
    "fixed-cache/std",
]
metrics = ["std", "dep:metrics", "dep:reth-metrics"]
test-utils = [
    "reth-primitives-traits/test-utils",
    "reth-trie-common/test-utils",
    "reth-ethereum-primitives/test-utils",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp"], default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
auto_impl = { version = "^1" }
derive_more = { version = "^2", features = ["full"], default-features = false }
fixed-cache = { version = "^0.1.10", features = ["alloc"], default-features = false }
futures-util = { version = "^0.3", default-features = false }
metrics = { version = "^0.24.0", optional = true }
rayon = { version = "^1.7", optional = true }
reth-execution-errors = { path = "../execution-errors", default-features = false }
reth-execution-types = { path = "../execution-types", default-features = false }
reth-metrics = { path = "../../metrics", optional = true }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-storage-api = { path = "../../storage/storage-api", default-features = false }
reth-storage-errors = { path = "../../storage/errors", default-features = false }
reth-trie-common = { path = "../../trie/common", default-features = false }
revm = { version = "^43.0.2", default-features = false }

[dev-dependencies]
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
