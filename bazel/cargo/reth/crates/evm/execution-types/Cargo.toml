# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/evm/execution-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-execution-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_execution_types"
path = "src/lib.rs"

[features]
default = ["std"]
serde = [
    "dep:serde",
    "rand/serde",
    "revm/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "reth-primitives-traits/serde",
    "alloy-consensus/serde",
    "reth-trie-common/serde",
    "reth-ethereum-primitives/serde",
]
serde-bincode-compat = [
    "serde",
    "alloy-rlp",
    "reth-trie-common/serde-bincode-compat",
    "serde_with",
    "alloy-consensus/serde-bincode-compat",
    "alloy-eips/serde-bincode-compat",
]
std = [
    "alloy-eips/std",
    "alloy-primitives/std",
    "revm/std",
    "serde?/std",
    "reth-primitives-traits/std",
    "alloy-consensus/std",
    "serde_with?/std",
    "derive_more/std",
    "reth-ethereum-primitives/std",
    "reth-trie-common/std",
    "alloy-evm/std",
    "alloy-rlp?/std",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], optional = true, default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-trie-common = { path = "../../trie/common", default-features = false }
revm = { version = "^43.0.2", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "rand", "arbitrary"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
arbitrary = { version = "^1.3" }
bincode = { version = "^1.3" }
rand = { version = "^0.9" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", features = ["arbitrary"], default-features = false }
reth-primitives-traits = { version = "^0.7.1", features = ["test-utils", "arbitrary"], default-features = false }
