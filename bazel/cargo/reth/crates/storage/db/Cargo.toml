# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/storage/db/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-db"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_db"
path = "src/lib.rs"

[features]
default = ["mdbx"]
mdbx = [
    "dep:reth-libmdbx",
    "dep:eyre",
    "dep:page_size",
    "dep:reth-primitives-traits",
    "reth-metrics",
    "dep:metrics",
    "dep:strum",
    "dep:rustc-hash",
]
test-utils = [
    "dep:tempfile",
    "mdbx",
    "arbitrary",
    "parking_lot",
    "reth-db-api/test-utils",
    "reth-nippy-jar/test-utils",
    "reth-primitives-traits/test-utils",
    "reth-prune-types/test-utils",
]
arbitrary = [
    "reth-db-api/arbitrary",
    "alloy-primitives/arbitrary",
    "alloy-consensus/arbitrary",
    "reth-primitives-traits/arbitrary",
    "reth-prune-types/arbitrary",
]
disable-lock = []

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
eyre = { version = "^0.6", optional = true }
metrics = { version = "^0.24.0", optional = true }
page_size = { version = "^0.6.0", optional = true }
parking_lot = { version = "^0.12", optional = true }
reth-db-api = { path = "../db-api" }
reth-fs-util = { path = "../../fs-util" }
reth-libmdbx = { path = "../libmdbx-rs", features = ["return-borrowed", "read-tx-timeouts"], optional = true }
reth-metrics = { path = "../../metrics", optional = true }
reth-nippy-jar = { path = "../nippy-jar" }
reth-primitives-traits = { version = "^0.7.1", features = ["quanta"], optional = true, default-features = false }
reth-static-file-types = { path = "../../static-file/types", default-features = false }
reth-storage-errors = { path = "../errors", default-features = false }
reth-tracing = { path = "../../tracing" }
rustc-hash = { version = "^2.0", features = ["std"], optional = true, default-features = false }
strum = { version = "^0.27", features = ["derive"], optional = true, default-features = false }
sysinfo = { version = "^0.38", features = ["system"], default-features = false }
tempfile = { version = "^3.20", optional = true }
thiserror = { version = "^2.0.0", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "getrandom"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
assert_matches = { version = "^1.5.0" }
parking_lot = { version = "^0.12" }
proptest = { version = "^1.7" }
reth-primitives-traits = { version = "^0.7.1", features = ["reth-codec"], default-features = false }
reth-prune-types = { path = "../../prune/types", default-features = false }
serde = { version = "^1.0", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tempfile = { version = "^3.20" }

[target.'cfg(unix)'.dependencies]
libc = { version = "^0.2" }
