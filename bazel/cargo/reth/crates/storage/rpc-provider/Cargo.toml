# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/storage/rpc-provider/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-storage-rpc-provider"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_storage_rpc_provider"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-network = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-provider = { version = "^2.4.2", features = ["reqwest", "debug-api", "debug-api"], default-features = false }
alloy-rpc-types = { version = "^2.4.2", features = ["eth"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
dashmap = { version = "^6.2", features = ["inline"] }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-db-api = { path = "../db-api" }
reth-errors = { path = "../../errors" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
reth-execution-types = { path = "../../evm/execution-types", default-features = false }
reth-node-types = { path = "../../node/types" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../provider" }
reth-prune-types = { path = "../../prune/types", default-features = false }
reth-stages-types = { path = "../../stages/types", default-features = false }
reth-storage-api = { path = "../storage-api", default-features = false }
reth-trie = { path = "../../trie/trie" }
revm = { version = "^43.0.2", default-features = false }
tokio = { version = "^1.52.3", features = ["sync", "macros", "rt-multi-thread"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
tokio = { version = "^1.52.3", features = ["rt", "macros"], default-features = false }
