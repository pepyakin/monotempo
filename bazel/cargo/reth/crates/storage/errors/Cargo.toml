# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/storage/errors/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-storage-errors"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_storage_errors"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-eips/std",
    "alloy-primitives/std",
    "alloy-rlp/std",
    "derive_more/std",
    "reth-primitives-traits/std",
    "thiserror/std",
    "reth-prune-types/std",
    "reth-static-file-types/std",
    "revm/std",
    "reth-codecs/std",
]

[dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-prune-types = { path = "../../prune/types", default-features = false }
reth-static-file-types = { path = "../../static-file/types", default-features = false }
revm = { version = "^43.0.2", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
