# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/storage/storage-api/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-storage-api"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_storage_api"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "reth-chainspec/std",
    "alloy-consensus/std",
    "alloy-eips/std",
    "alloy-primitives/std",
    "alloy-rpc-types-engine/std",
    "reth-primitives-traits/std",
    "reth-stages-types/std",
    "revm/std",
    "reth-ethereum-primitives/std",
    "reth-execution-types/std",
    "reth-prune-types/std",
    "reth-storage-errors/std",
    "reth-db-models/std",
    "reth-trie-common/std",
    "serde_json?/std",
    "dep:reth-tokio-util",
    "alloy-eip7928/std",
]
db-api = ["dep:reth-db-api", "dep:serde_json"]
serde = [
    "reth-ethereum-primitives/serde",
    "reth-db-models/serde",
    "reth-execution-types/serde",
    "reth-primitives-traits/serde",
    "reth-prune-types/serde",
    "reth-stages-types/serde",
    "reth-trie-common/serde",
    "revm/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "alloy-rpc-types-engine/serde",
    "alloy-eip7928/serde",
]
serde-bincode-compat = [
    "reth-execution-types/serde-bincode-compat",
    "reth-trie-common/serde-bincode-compat",
    "alloy-eips/serde-bincode-compat",
    "alloy-consensus/serde-bincode-compat",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp"], default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
auto_impl = { version = "^1" }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-db-api = { path = "../db-api", optional = true }
reth-db-models = { path = "../db-models", default-features = false }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
reth-execution-types = { path = "../../evm/execution-types", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-prune-types = { path = "../../prune/types", default-features = false }
reth-stages-types = { path = "../../stages/types", default-features = false }
reth-storage-errors = { path = "../errors", default-features = false }
reth-tokio-util = { path = "../../tokio-util", optional = true }
reth-trie-common = { path = "../../trie/common", default-features = false }
revm = { version = "^43.0.2", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], optional = true, default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "getrandom"], default-features = false }
tokio = { version = "^1.52.3", features = ["macros", "rt"], default-features = false }
tokio-stream = { version = "^0.1.11" }
