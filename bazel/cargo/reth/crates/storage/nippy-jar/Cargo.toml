# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/storage/nippy-jar/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-nippy-jar"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_nippy_jar"
path = "src/lib.rs"

[features]
default = []
test-utils = []

[dependencies]
anyhow = { version = "^1.0" }
bincode = { version = "^1.3" }
derive_more = { version = "^2", features = ["full"], default-features = false }
lz4_flex = { version = "^0.12", default-features = false }
memmap2 = { version = "^0.9.11" }
reth-fs-util = { path = "../../fs-util" }
serde = { version = "^1.0", features = ["derive"], default-features = false }
smallvec = { version = "^1" }
thiserror = { version = "^2.0.0", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
zstd = { version = "^0.13", features = ["experimental", "zdict_builder"] }

[dev-dependencies]
rand = { version = "^0.9", features = ["small_rng"] }
tempfile = { version = "^3.20" }
