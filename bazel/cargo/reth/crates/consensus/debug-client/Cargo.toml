# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/consensus/debug-client/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-consensus-debug-client"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_consensus_debug_client"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-json-rpc = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-provider = { version = "^2.4.2", features = ["reqwest", "debug-api", "ws"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
alloy-transport = { version = "^2.4.2" }
auto_impl = { version = "^1" }
derive_more = { version = "^2", features = ["full"], default-features = false }
eyre = { version = "^0.6" }
futures = { version = "^0.3" }
reqwest = { version = "^0.13", features = ["rustls", "stream", "query"], default-features = false }
reth-node-api = { path = "../../node/api" }
reth-tracing = { path = "../../tracing" }
ringbuffer = { version = "^0.16.0" }
serde = { version = "^1.0", features = ["derive"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1.52.3", features = ["time"], default-features = false }
