# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/consensus/common/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-consensus-common"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_consensus_common"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-consensus/std",
    "alloy-eips/std",
    "reth-chainspec/std",
    "reth-consensus/std",
    "reth-primitives-traits/std",
    "reth-ethereum-primitives/std",
    "alloy-primitives/std",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-consensus = { path = "../consensus", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "rand"], default-features = false }
rand = { version = "^0.9" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
