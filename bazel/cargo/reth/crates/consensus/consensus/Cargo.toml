# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/consensus/consensus/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-consensus"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_consensus"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "reth-primitives-traits/std",
    "alloy-primitives/std",
    "alloy-consensus/std",
    "reth-execution-types/std",
    "thiserror/std",
    "alloy-eip7928/std",
]
test-utils = ["reth-primitives-traits/test-utils"]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp"], default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
auto_impl = { version = "^1" }
reth-execution-types = { path = "../../evm/execution-types", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
