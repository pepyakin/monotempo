# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/tokio-util/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-tokio-util"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_tokio_util"
path = "src/lib.rs"

[features]
time = ["tokio/time"]

[dependencies]
tokio = { version = "^1.52.3", features = ["sync"], default-features = false }
tokio-stream = { version = "^0.1.11", features = ["sync"] }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
tokio = { version = "^1.52.3", features = ["full", "macros"], default-features = false }
