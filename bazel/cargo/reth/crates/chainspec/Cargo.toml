# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/chainspec/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-chainspec"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_chainspec"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-chains/std",
    "alloy-eips/std",
    "alloy-genesis/std",
    "alloy-primitives/std",
    "alloy-trie/std",
    "reth-primitives-traits/std",
    "alloy-consensus/std",
    "alloy-rlp/std",
    "reth-ethereum-forks/std",
    "derive_more/std",
    "reth-network-peers/std",
    "serde_json/std",
    "alloy-evm/std",
]
arbitrary = [
    "alloy-chains/arbitrary",
    "reth-ethereum-forks/arbitrary",
    "reth-primitives-traits/arbitrary",
    "alloy-consensus/arbitrary",
    "alloy-eips/arbitrary",
    "alloy-primitives/arbitrary",
    "alloy-trie/arbitrary",
]
test-utils = ["reth-primitives-traits/test-utils"]

[dependencies]
alloy-chains = { version = "^0.2.33", features = ["serde", "rlp"], default-features = false }
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", features = ["serde"], default-features = false }
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-genesis = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "rlp"], default-features = false }
alloy-trie = { version = "^0.9.4", features = ["ethereum"], default-features = false }
auto_impl = { version = "^1" }
derive_more = { version = "^2", features = ["full"], default-features = false }
reth-ethereum-forks = { path = "../ethereum/hardforks", default-features = false }
reth-network-peers = { path = "../net/peers", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }

[dev-dependencies]
alloy-eips = { version = "^2.4.2", features = ["arbitrary"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net", "arrayvec"], default-features = false }
alloy-trie = { version = "^0.9.4", features = ["arbitrary"], default-features = false }
