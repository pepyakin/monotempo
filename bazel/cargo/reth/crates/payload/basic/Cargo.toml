# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/payload/basic/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-basic-payload-builder"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_basic_payload_builder"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
futures-core = { version = "^0.3" }
futures-util = { version = "^0.3", default-features = false }
metrics = { version = "^0.24.0" }
reth-chain-state = { path = "../../chain-state" }
reth-execution-cache = { path = "../../engine/execution-cache" }
reth-metrics = { path = "../../metrics" }
reth-payload-builder = { path = "../builder" }
reth-payload-builder-primitives = { path = "../builder-primitives" }
reth-payload-primitives = { path = "../primitives" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-revm = { path = "../../revm", default-features = false }
reth-storage-api = { path = "../../storage/storage-api", default-features = false }
reth-tasks = { path = "../../tasks" }
reth-trie-parallel = { path = "../../trie/parallel" }
serde = { version = "^1.0", default-features = false }
tokio = { version = "^1.52.3", features = ["sync", "time"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
