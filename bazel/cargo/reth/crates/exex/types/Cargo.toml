# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/exex/types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-exex-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_exex_types"
path = "src/lib.rs"

[features]
default = []
serde = [
    "dep:serde",
    "reth-execution-types/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "rand/serde",
    "reth-primitives-traits/serde",
    "reth-ethereum-primitives/serde",
    "reth-chain-state/serde",
]
serde-bincode-compat = ["reth-execution-types/serde-bincode-compat", "serde_with", "alloy-eips/serde-bincode-compat"]

[dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
reth-chain-state = { path = "../../chain-state" }
reth-execution-types = { path = "../../evm/execution-types", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }

[dev-dependencies]
arbitrary = { version = "^1.3" }
bincode = { version = "^1.3" }
rand = { version = "^0.9" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", features = ["arbitrary", "std"], default-features = false }
reth-primitives-traits = { version = "^0.7.1", features = ["arbitrary"], default-features = false }
