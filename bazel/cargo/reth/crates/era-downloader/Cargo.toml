# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/era-downloader/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-era-downloader"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_era_downloader"
path = "src/lib.rs"

[[test]]
name = "it"
path = "tests/it/main.rs"

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
bytes = { version = "^1.11.1", default-features = false }
eyre = { version = "^0.6" }
futures-util = { version = "^0.3", default-features = false }
reqwest = { version = "^0.13", features = ["rustls", "stream"], default-features = false }
reth-era = { path = "../era" }
reth-fs-util = { path = "../fs-util" }
sha2 = { version = "^0.10", features = ["std"], default-features = false }
tokio = { version = "^1.52.3", features = ["fs", "io-util", "macros"], default-features = false }

[dev-dependencies]
futures = { version = "^0.3" }
tempfile = { version = "^3.20" }
test-case = { version = "^3" }
