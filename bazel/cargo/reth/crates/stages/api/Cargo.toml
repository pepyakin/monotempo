# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/stages/api/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-stages-api"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_stages_api"
path = "src/lib.rs"

[features]
test-utils = [
    "reth-chainspec/test-utils",
    "reth-consensus/test-utils",
    "reth-db-api/test-utils",
    "reth-db/test-utils",
    "reth-network-p2p/test-utils",
    "reth-primitives-traits/test-utils",
    "reth-provider/test-utils",
    "reth-stages-types/test-utils",
    "reth-tasks/test-utils",
    "reth-codecs/test-utils",
]

[dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
aquamarine = { version = "^0.6" }
auto_impl = { version = "^1" }
futures-util = { version = "^0.3", default-features = false }
metrics = { version = "^0.24.0" }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-consensus = { path = "../../consensus/consensus", default-features = false }
reth-errors = { path = "../../errors" }
reth-metrics = { path = "../../metrics" }
reth-network-p2p = { path = "../../net/p2p" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../../storage/provider" }
reth-prune = { path = "../../prune/prune" }
reth-stages-types = { path = "../types", default-features = false }
reth-static-file = { path = "../../static-file/static-file" }
reth-static-file-types = { path = "../../static-file/types", default-features = false }
reth-tokio-util = { path = "../../tokio-util" }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", features = ["sync"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
assert_matches = { version = "^1.5.0" }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-db = { path = "../../storage/db", features = ["test-utils"], default-features = false }
reth-db-api = { path = "../../storage/db-api" }
reth-provider = { path = "../../storage/provider", features = ["test-utils"] }
reth-tasks = { path = "../../tasks" }
reth-testing-utils = { path = "../../../testing/testing-utils" }
tokio = { version = "^1.52.3", features = ["sync", "rt-multi-thread"], default-features = false }
tokio-stream = { version = "^0.1.11" }
