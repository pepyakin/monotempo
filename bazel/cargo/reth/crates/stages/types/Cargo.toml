# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/stages/types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-stages-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_stages_types"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "bytes?/std", "reth-trie-common/std", "serde?/std", "reth-codecs?/std"]
reth-codec = ["dep:reth-codecs", "dep:bytes", "dep:modular-bitfield", "reth-trie-common/reth-codec"]
test-utils = ["arbitrary", "reth-codecs?/test-utils", "reth-trie-common/test-utils"]
arbitrary = [
    "std",
    "dep:arbitrary",
    "alloy-primitives/arbitrary",
    "reth-codecs?/arbitrary",
    "reth-trie-common/arbitrary",
]
serde = [
    "dep:serde",
    "alloy-primitives/serde",
    "bytes?/serde",
    "rand/serde",
    "reth-codecs?/serde",
    "reth-trie-common/serde",
]

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
bytes = { version = "^1.11.1", optional = true, default-features = false }
modular-bitfield = { version = "^0.13.1", optional = true }
reth-codecs = { version = "^0.7.0", optional = true, default-features = false }
reth-trie-common = { path = "../../trie/common", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "arbitrary", "rand"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
bytes = { version = "^1.11.1", default-features = false }
modular-bitfield = { version = "^0.13.1" }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
rand = { version = "^0.9" }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-trie-common = { path = "../../trie/common", features = ["reth-codec"], default-features = false }
