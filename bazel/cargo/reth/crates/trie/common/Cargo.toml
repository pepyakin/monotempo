# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/trie/common/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-trie-common"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_trie_common"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-consensus/std",
    "alloy-genesis/std",
    "alloy-primitives/std",
    "alloy-rlp/std",
    "alloy-rpc-types-eth?/std",
    "alloy-serde?/std",
    "alloy-trie/std",
    "arrayvec?/std",
    "bytes?/std",
    "derive_more/std",
    "nybbles/std",
    "reth-primitives-traits/std",
    "serde?/std",
    "serde_with?/std",
    "serde_json/std",
    "thiserror/std",
    "revm/std",
    "reth-codecs?/std",
    "alloy-eips/std",
]
eip1186 = ["alloy-rpc-types-eth/serde", "dep:alloy-serde"]
serde = [
    "dep:serde",
    "arrayvec?/serde",
    "bytes?/serde",
    "nybbles/serde",
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "alloy-trie/serde",
    "alloy-rpc-types-eth?/serde",
    "reth-primitives-traits/serde",
    "reth-codecs?/serde",
    "revm/serde",
    "alloy-eips/serde",
]
reth-codec = ["dep:reth-codecs", "dep:bytes", "dep:arrayvec"]
serde-bincode-compat = [
    "serde",
    "alloy-consensus/serde-bincode-compat",
    "dep:serde_with",
    "alloy-genesis/serde-bincode-compat",
    "alloy-rpc-types-eth?/serde-bincode-compat",
    "alloy-eips/serde-bincode-compat",
]
test-utils = [
    "dep:plain_hasher",
    "dep:hash-db",
    "arbitrary",
    "reth-primitives-traits/test-utils",
    "reth-codecs/test-utils",
]
arbitrary = [
    "std",
    "dep:reth-codecs",
    "alloy-trie/arbitrary",
    "dep:arbitrary",
    "alloy-serde?/arbitrary",
    "reth-primitives-traits/arbitrary",
    "alloy-consensus/arbitrary",
    "alloy-primitives/arbitrary",
    "nybbles/arbitrary",
    "reth-codecs/arbitrary",
    "alloy-rpc-types-eth?/arbitrary",
    "alloy-eips/arbitrary",
    "revm/arbitrary",
]
rayon = ["dep:rayon"]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net", "arrayvec"], default-features = false }
alloy-rpc-types-eth = { version = "^2.4.2", optional = true, default-features = false }
alloy-serde = { version = "^2.4.2", optional = true, default-features = false }
alloy-trie = { version = "^0.9.4", features = ["ethereum"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
arrayvec = { version = "^0.7.6", optional = true, default-features = false }
bytes = { version = "^1.11.1", optional = true, default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
hash-db = { version = "=0.15.2", optional = true }
itertools = { version = "^0.14", features = ["use_alloc"], default-features = false }
nybbles = { version = "^0.4.8", features = ["rlp"], default-features = false }
plain_hasher = { version = "^0.2", optional = true }
rayon = { version = "^1.7", optional = true }
reth-codecs = { version = "^0.7.0", optional = true, default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
revm = { version = "^43.0.2", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-genesis = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "getrandom"], default-features = false }
alloy-trie = { version = "^0.9.4", features = ["arbitrary", "serde"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
arrayvec = { version = "^0.7.6", default-features = false }
bincode = { version = "^1.3" }
bytes = { version = "^1.11.1", default-features = false }
hash-db = { version = "=0.15.2" }
plain_hasher = { version = "^0.2" }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-primitives-traits = { version = "^0.7.1", features = ["serde"], default-features = false }
serde = { version = "^1.0", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
serde_with = { version = "^3", features = ["macros"], default-features = false }
