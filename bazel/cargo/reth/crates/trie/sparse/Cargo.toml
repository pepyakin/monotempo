# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/trie/sparse/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-trie-sparse"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_trie_sparse"
path = "src/lib.rs"

[[test]]
name = "suite"
path = "tests/suite/main.rs"

[features]
default = ["std", "metrics"]
std = [
    "dep:either",
    "dep:rayon",
    "dep:reth-primitives-traits",
    "dep:smallvec",
    "dep:slotmap",
    "dep:tracing",
    "alloy-primitives/std",
    "alloy-rlp/std",
    "alloy-trie/std",
    "reth-execution-errors/std",
    "reth-primitives-traits/std",
    "reth-primitives-traits/rayon",
    "reth-trie-common/std",
    "tracing/std",
    "strum/std",
    "reth-tracing/std",
    "either/std",
]
metrics = ["dep:reth-metrics", "dep:metrics", "std"]
test-utils = ["std", "reth-primitives-traits/test-utils", "reth-trie-common/test-utils", "reth-trie/test-utils"]
arbitrary = [
    "std",
    "alloy-primitives/arbitrary",
    "alloy-trie/arbitrary",
    "reth-primitives-traits/arbitrary",
    "reth-trie-common/arbitrary",
    "smallvec/arbitrary",
]

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-trie = { version = "^0.9.4", default-features = false }
either = { version = "^1.16.0", optional = true, default-features = false }
metrics = { version = "^0.24.0", optional = true }
rayon = { version = "^1.7", optional = true }
reth-execution-errors = { path = "../../evm/execution-errors", default-features = false }
reth-metrics = { path = "../../metrics", optional = true }
reth-primitives-traits = { version = "^0.7.1", optional = true, default-features = false }
reth-trie-common = { path = "../common", default-features = false }
slotmap = { version = "^1", optional = true }
smallvec = { version = "^1", optional = true }
strum = { version = "^0.27", features = ["derive"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes", "attributes"], optional = true, default-features = false }

[dev-dependencies]
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
arbitrary = { version = "^1.3" }
itertools = { version = "^0.14", default-features = false }
pretty_assertions = { version = "^1.4" }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
rand = { version = "^0.9" }
rand_08 = { package = "rand", version = "^0.8" }
reth-primitives-traits = { version = "^0.7.1", features = ["arbitrary"], default-features = false }
reth-tracing = { path = "../../tracing" }
reth-trie = { path = "../trie", features = ["test-utils"] }
reth-trie-common = { path = "../common", features = ["test-utils", "arbitrary"], default-features = false }
