# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/trie/trie/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-trie"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_trie"
path = "src/lib.rs"

[[bench]]
name = "proof_v2"
path = "benches/proof_v2.rs"
required-features = ["test-utils"]

[features]
metrics = ["reth-metrics", "dep:metrics"]
serde = [
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "alloy-trie/serde",
    "alloy-eips/serde",
    "reth-trie-common/serde",
    "reth-primitives-traits/serde",
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "alloy-trie/serde",
    "alloy-eips/serde",
    "reth-trie-common/serde",
    "reth-stages-types/serde",
    "parking_lot/serde",
    "rand/serde",
    "revm/serde",
]
test-utils = [
    "triehash",
    "parking_lot",
    "reth-primitives-traits/test-utils",
    "reth-trie-common/test-utils",
    "reth-trie-sparse/test-utils",
    "reth-stages-types/test-utils",
]
serde-bincode-compat = [
    "alloy-consensus/serde-bincode-compat",
    "alloy-eips/serde-bincode-compat",
    "reth-trie-common/serde-bincode-compat",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
alloy-trie = { version = "^0.9.4", default-features = false }
auto_impl = { version = "^1" }
itertools = { version = "^0.14", default-features = false }
metrics = { version = "^0.24.0", optional = true }
parking_lot = { version = "^0.12", optional = true }
reth-execution-errors = { path = "../../evm/execution-errors", default-features = false }
reth-metrics = { path = "../../metrics", optional = true }
reth-primitives-traits = { version = "^0.7.1", features = ["std"], default-features = false }
reth-stages-types = { path = "../../stages/types", default-features = false }
reth-storage-errors = { path = "../../storage/errors", default-features = false }
reth-trie-common = { path = "../common", features = ["rayon"], default-features = false }
reth-trie-sparse = { path = "../sparse", features = ["std"], default-features = false }
revm = { version = "^43.0.2", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes", "attributes"], default-features = false }
triehash = { version = "^0.8", optional = true }

[dev-dependencies]
assert_matches = { version = "^1.5.0" }
codspeed-criterion-compat = { version = "^4.3" }
parking_lot = { version = "^0.12" }
pretty_assertions = { version = "^1.4" }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
rand = { version = "^0.9" }
reth-primitives-traits = { version = "^0.7.1", features = ["test-utils"], default-features = false }
reth-tracing = { path = "../../tracing" }
reth-trie-common = { path = "../common", features = ["test-utils", "arbitrary"], default-features = false }
triehash = { version = "^0.8" }
