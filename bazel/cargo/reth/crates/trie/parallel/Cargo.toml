# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/trie/parallel/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-trie-parallel"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_trie_parallel"
path = "src/lib.rs"

[features]
default = ["metrics"]
metrics = ["reth-metrics", "dep:metrics", "reth-trie/metrics"]
trie-debug = ["dep:rand"]
test-utils = [
    "reth-primitives-traits/test-utils",
    "reth-provider/test-utils",
    "reth-trie/test-utils",
    "reth-tasks/test-utils",
    "reth-chainspec/test-utils",
    "reth-ethereum-primitives/test-utils",
]

[dependencies]
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
crossbeam-channel = { version = "^0.5.13" }
crossbeam-utils = { version = "^0.8" }
metrics = { version = "^0.24.0", optional = true }
rand = { version = "^0.9", optional = true }
reth-execution-errors = { path = "../../evm/execution-errors", default-features = false }
reth-metrics = { path = "../../metrics", optional = true }
reth-primitives-traits = { version = "^0.7.1", features = ["dashmap", "std"], default-features = false }
reth-provider = { path = "../../storage/provider" }
reth-storage-errors = { path = "../../storage/errors", default-features = false }
reth-tasks = { path = "../../tasks", features = ["rayon"] }
reth-trie = { path = "../trie" }
revm = { version = "^43.0.2", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
rand = { version = "^0.9" }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../../storage/provider", features = ["test-utils"] }
reth-storage-overlay = { path = "../../storage/storage-overlay" }
reth-trie = { path = "../trie", features = ["test-utils"] }
tokio = { version = "^1.52.3", features = ["rt", "rt-multi-thread", "macros"], default-features = false }
