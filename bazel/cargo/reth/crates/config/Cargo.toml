# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/config/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-config"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_config"
path = "src/lib.rs"

[features]
serde = [
    "dep:serde",
    "dep:toml",
    "dep:humantime-serde",
    "reth-network-peers/secp256k1",
    "reth-network-types/serde",
    "reth-prune-types/serde",
    "reth-stages-types/serde",
    "alloy-primitives/serde",
    "url/serde",
]

[dependencies]
eyre = { version = "^0.6" }
humantime-serde = { version = "^1.1", optional = true }
reth-network-peers = { path = "../net/peers", default-features = false }
reth-network-types = { path = "../net/network-types" }
reth-prune-types = { path = "../prune/types", default-features = false }
reth-stages-types = { path = "../stages/types", default-features = false }
reth-static-file-types = { path = "../static-file/types", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
toml = { version = "^0.9", optional = true }
url = { version = "^2.3", default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "getrandom"], default-features = false }
tempfile = { version = "^3.20" }
