# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/eth-wire-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-eth-wire-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_eth_wire_types"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-chains/std",
    "alloy-consensus/std",
    "alloy-eip7928?/std",
    "alloy-eips/std",
    "alloy-genesis/std",
    "alloy-primitives/std",
    "alloy-rlp/std",
    "bytes/std",
    "derive_more/std",
    "reth-ethereum-primitives/std",
    "reth-primitives-traits/std",
    "serde?/std",
    "thiserror/std",
    "reth-chainspec/std",
    "alloy-trie/std",
]
arbitrary = [
    "reth-ethereum-primitives/arbitrary",
    "alloy-chains/arbitrary",
    "dep:alloy-eip7928",
    "alloy-eip7928/arbitrary",
    "dep:arbitrary",
    "dep:proptest",
    "dep:proptest-arbitrary-interop",
    "reth-chainspec/arbitrary",
    "alloy-consensus/arbitrary",
    "alloy-eips/arbitrary",
    "alloy-primitives/arbitrary",
    "reth-primitives-traits/arbitrary",
    "alloy-trie/arbitrary",
]
serde = [
    "dep:serde",
    "alloy-chains/serde",
    "alloy-consensus/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "bytes/serde",
    "rand/serde",
    "reth-primitives-traits/serde",
    "reth-ethereum-primitives/serde",
    "alloy-hardforks/serde",
    "alloy-eip7928?/serde",
    "alloy-trie/serde",
]

[dependencies]
alloy-chains = { version = "^0.2.33", features = ["rlp"], default-features = false }
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp", "rlp"], optional = true, default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-hardforks = { version = "^0.4.8" }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "map"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net", "derive"], default-features = false }
alloy-trie = { version = "^0.9.4", default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
bytes = { version = "^1.11.1", default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
proptest = { version = "^1.7", optional = true }
proptest-arbitrary-interop = { version = "^0.1.0", optional = true }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-codecs-derive = { version = "^0.7.0" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
alloy-chains = { version = "^0.2.33", features = ["arbitrary"], default-features = false }
alloy-consensus = { version = "^2.4.2", features = ["arbitrary"], default-features = false }
alloy-eip7928 = { version = "^0.4.10", features = ["rlp", "arbitrary", "rlp"], default-features = false }
alloy-eips = { version = "^2.4.2", features = ["arbitrary"], default-features = false }
alloy-genesis = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "arbitrary", "rand"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
rand = { version = "^0.9" }
reth-ethereum-primitives = { path = "../../ethereum/primitives", features = ["arbitrary"], default-features = false }
test-case = { version = "^3" }
