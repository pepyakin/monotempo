# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/network-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-network-types"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_network_types"
path = "src/lib.rs"

[features]
serde = ["dep:serde", "dep:humantime-serde", "dep:serde_json", "alloy-eip2124/serde"]
test-utils = []

[dependencies]
alloy-eip2124 = { version = "^0.2.0", default-features = false }
humantime-serde = { version = "^1.1", optional = true }
reth-net-banlist = { path = "../banlist" }
reth-network-peers = { path = "../peers", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
serde_json = { version = "^1.0", features = ["alloc", "std"], optional = true, default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
