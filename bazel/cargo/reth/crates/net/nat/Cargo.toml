# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/nat/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-net-nat"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_net_nat"
path = "src/lib.rs"

[features]
default = ["serde"]
serde = ["dep:serde_with"]

[dependencies]
futures-util = { version = "^0.3", default-features = false }
if-addrs = { version = "^0.14" }
reqwest = { version = "^0.13", features = ["rustls", "stream"], default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", features = ["time"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
reth-tracing = { path = "../../tracing" }
tokio = { version = "^1.52.3", features = ["macros"], default-features = false }
