# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/eth-wire/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-eth-wire"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_eth_wire"
path = "src/lib.rs"

[[test]]
name = "fuzz_roundtrip"
path = "tests/fuzz_roundtrip.rs"
required-features = ["arbitrary", "serde"]

[[test]]
name = "new_block"
path = "tests/new_block.rs"

[[test]]
name = "new_pooled_transactions"
path = "tests/new_pooled_transactions.rs"

[[test]]
name = "pooled_transactions"
path = "tests/pooled_transactions.rs"

[features]
arbitrary = [
    "reth-eth-wire-types/arbitrary",
    "dep:arbitrary",
    "alloy-eips/arbitrary",
    "alloy-primitives/arbitrary",
    "reth-codecs/arbitrary",
    "alloy-chains/arbitrary",
    "reth-primitives-traits/arbitrary",
    "reth-ethereum-forks/arbitrary",
    "alloy-consensus/arbitrary",
]
serde = [
    "dep:serde",
    "reth-eth-wire-types/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "bytes/serde",
    "rand/serde",
    "secp256k1/serde",
    "reth-codecs/serde",
    "alloy-chains/serde",
    "reth-primitives-traits/serde",
    "reth-ethereum-forks/serde",
    "alloy-consensus/serde",
    "rand_08/serde",
]

[dependencies]
alloy-chains = { version = "^0.2.33", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net", "derive"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
bytes = { version = "^1.11.1", default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
futures = { version = "^0.3" }
pin-project = { version = "^1.1.12" }
reth-codecs = { version = "^0.7.0", default-features = false }
reth-ecies = { path = "../ecies" }
reth-eth-wire-types = { path = "../eth-wire-types" }
reth-ethereum-forks = { path = "../../ethereum/hardforks", default-features = false }
reth-metrics = { path = "../../metrics" }
reth-network-peers = { path = "../peers", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
snap = { version = "^1.1.1" }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", features = ["macros", "net", "sync", "time"], default-features = false }
tokio-stream = { version = "^0.1.11" }
tokio-util = { version = "^0.7.4", features = ["codec", "io", "codec"] }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
async-stream = { version = "^0.3" }
proptest = { version = "^1.7" }
proptest-arbitrary-interop = { version = "^0.1.0" }
rand = { version = "^0.9" }
rand_08 = { package = "rand", version = "^0.8" }
reth-eth-wire-types = { path = "../eth-wire-types", features = ["arbitrary"] }
reth-primitives-traits = { version = "^0.7.1", features = ["arbitrary"], default-features = false }
reth-tracing = { path = "../../tracing" }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "global-context", "std", "recovery"], default-features = false }
serde = { version = "^1.0", default-features = false }
test-fuzz = { version = "^7" }
tokio = { version = "^1.52.3", features = ["rt", "rt-multi-thread"], default-features = false }
