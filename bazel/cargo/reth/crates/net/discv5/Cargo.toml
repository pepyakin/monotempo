# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/discv5/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-discv5"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_discv5"
path = "src/lib.rs"

[dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
derive_more = { version = "^2", features = ["full"], default-features = false }
discv5 = { git = "https://github.com/sigp/discv5", rev = "7663c00", features = ["libp2p"] }
enr = { version = "^0.13", default-features = false }
futures = { version = "^0.3" }
itertools = { version = "^0.14", default-features = false }
metrics = { version = "^0.24.0" }
rand = { version = "^0.9" }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-ethereum-forks = { path = "../../ethereum/hardforks", default-features = false }
reth-metrics = { path = "../../metrics" }
reth-network-peers = { path = "../peers", features = ["secp256k1"], default-features = false }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery"], default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "rand"], default-features = false }
rand_08 = { package = "rand", version = "^0.8" }
reth-tracing = { path = "../../tracing" }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "std", "rand"], default-features = false }
tokio = { version = "^1.52.3", features = ["rt-multi-thread"], default-features = false }
