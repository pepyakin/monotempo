# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/net/ecies/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-ecies"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_ecies"
path = "src/lib.rs"

[dependencies]
aes = { version = "^0.8.1" }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash", "rand", "rlp"], default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net", "derive", "arrayvec"], default-features = false }
byteorder = { version = "^1" }
cipher = { version = "^0.4.3" }
concat-kdf = { version = "^0.1.0" }
ctr = { version = "^0.9.2" }
digest = { version = "^0.10.5" }
futures = { version = "^0.3" }
hmac = { version = "^0.12.1" }
pin-project = { version = "^1.1.12" }
rand_08 = { package = "rand", version = "^0.8" }
reth-network-peers = { path = "../peers", features = ["secp256k1"], default-features = false }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "global-context", "std", "recovery", "rand"], default-features = false }
sha2 = { version = "^0.10", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", features = ["time"], default-features = false }
tokio-stream = { version = "^0.1.11" }
tokio-util = { version = "^0.7.4", features = ["codec", "codec"] }
tracing = { version = "^0.1.0", features = ["attributes", "attributes"], default-features = false }

[dev-dependencies]
tokio = { version = "^1.52.3", features = ["net", "rt", "macros"], default-features = false }
