# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/ethereum/evm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-evm-ethereum"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_evm_ethereum"
path = "src/lib.rs"

[[test]]
name = "execute"
path = "tests/execute.rs"

[features]
default = ["std"]
std = [
    "alloy-consensus/std",
    "alloy-eips/std",
    "alloy-genesis/std",
    "alloy-primitives/std",
    "secp256k1/std",
    "reth-ethereum-forks/std",
    "reth-chainspec/std",
    "alloy-evm/std",
    "reth-execution-types/std",
    "reth-evm/std",
    "reth-primitives-traits/std",
    "revm/std",
    "reth-ethereum-primitives/std",
    "alloy-rpc-types-engine/std",
    "reth-storage-errors/std",
]
jit = ["std", "dep:revmc", "dep:metrics", "dep:reth-metrics"]
gmp = ["revm/gmp"]
test-utils = [
    "std",
    "reth-chainspec/test-utils",
    "reth-ethereum-primitives/test-utils",
    "reth-evm/test-utils",
    "reth-primitives-traits/test-utils",
]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
metrics = { version = "^0.24.0", optional = true }
reth-chainspec = { path = "../../chainspec", default-features = false }
reth-ethereum-forks = { path = "../hardforks", default-features = false }
reth-ethereum-primitives = { path = "../primitives", default-features = false }
reth-evm = { path = "../../evm/evm", default-features = false }
reth-execution-types = { path = "../../evm/execution-types", default-features = false }
reth-metrics = { path = "../../metrics", optional = true }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-storage-errors = { path = "../../storage/errors", default-features = false }
revm = { version = "^43.0.2", default-features = false }
revmc = { git = "https://github.com/paradigmxyz/revmc", branch = "main", features = ["llvm-prefer-static", "alloy-evm"], optional = true, default-features = false }

[dev-dependencies]
alloy-genesis = { version = "^2.4.2", default-features = false }
reth-evm = { path = "../../evm/evm", features = ["test-utils"], default-features = false }
reth-testing-utils = { path = "../../../testing/testing-utils" }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery"], default-features = false }
