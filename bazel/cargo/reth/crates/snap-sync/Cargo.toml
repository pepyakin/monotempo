# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/crates/snap-sync/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-snap-sync"
version = "2.5.2"
edition = "2024"
rust-version = "1.95"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_snap_sync"
path = "src/lib.rs"

[dependencies]
alloy-eip7928 = { version = "^0.4.10", features = ["rlp"], default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
reth-db-api = { path = "../storage/db-api" }
reth-downloaders = { path = "../net/downloaders" }
reth-eth-wire-types = { path = "../net/eth-wire-types" }
reth-network-p2p = { path = "../net/p2p" }
reth-network-peers = { path = "../net/peers", default-features = false }
reth-primitives-traits = { version = "^0.7.1", features = ["eip7928"], default-features = false }
reth-storage-api = { path = "../storage/storage-api", features = ["db-api"], default-features = false }
reth-storage-errors = { path = "../storage/errors", default-features = false }
reth-tasks = { path = "../tasks" }
reth-trie-common = { path = "../trie/common", default-features = false }
revm = { version = "^43.0.2", default-features = false }
serde = { version = "^1.0", features = ["derive"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio-util = { version = "^0.7.4", features = ["codec"] }

[dev-dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-rlp = { version = "^0.3.16", features = ["core-net"], default-features = false }
futures = { version = "^0.3" }
reth-chainspec = { path = "../chainspec", default-features = false }
reth-ethereum-primitives = { path = "../ethereum/primitives", default-features = false }
reth-evm = { path = "../evm/evm", default-features = false }
reth-evm-ethereum = { path = "../ethereum/evm", default-features = false }
reth-provider = { path = "../storage/provider", features = ["test-utils"] }
tokio = { version = "^1.52.3", features = ["macros", "rt-multi-thread"], default-features = false }
