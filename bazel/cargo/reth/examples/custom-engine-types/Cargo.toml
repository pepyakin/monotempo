# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/custom-engine-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-custom-engine-types"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-custom-engine-types"
path = "src/main.rs"

[dependencies]
alloy-genesis = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types = { version = "^2.4.2", features = ["eth", "engine"], default-features = false }
eyre = { version = "^0.6" }
reth-basic-payload-builder = { path = "../../crates/payload/basic" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["test-utils", "node", "node-api", "pool"] }
reth-ethereum-payload-builder = { path = "../../crates/ethereum/payload" }
reth-payload-builder = { path = "../../crates/payload/builder" }
reth-tracing = { path = "../../crates/tracing" }
serde = { version = "^1.0", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tokio = { version = "^1.52.3", default-features = false }
