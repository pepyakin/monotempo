# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/manual-p2p/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-manual-p2p"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-manual-p2p"
path = "src/main.rs"

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
eyre = { version = "^0.6" }
futures = { version = "^0.3" }
reth-discv4 = { path = "../../crates/net/discv4" }
reth-ecies = { path = "../../crates/net/ecies" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["network"] }
reth-network-peers = { path = "../../crates/net/peers", default-features = false }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "global-context", "std", "recovery"], default-features = false }
tokio = { version = "^1.52.3", default-features = false }
