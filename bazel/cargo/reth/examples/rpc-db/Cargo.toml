# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/rpc-db/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-rpc-db"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-rpc-db"
path = "src/main.rs"

[dependencies]
eyre = { version = "^0.6" }
futures = { version = "^0.3" }
jsonrpsee = { version = "^0.26.0" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["test-utils", "node", "provider", "pool", "network", "rpc"] }
tokio = { version = "^1.52.3", features = ["full"], default-features = false }
