# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/custom-state-root/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-custom-state-root"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-custom-state-root"
path = "src/main.rs"

[dependencies]
alloy-genesis = { version = "^2.4.2", default-features = false }
alloy-network = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-engine = { version = "^2.4.2", default-features = false }
eyre = { version = "^0.6" }
reth-engine-tree = { path = "../../crates/engine/tree" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "test-utils"] }
reth-evm = { path = "../../crates/evm/evm", default-features = false }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-provider = { path = "../../crates/storage/provider" }
reth-storage-overlay = { path = "../../crates/storage/storage-overlay" }
reth-trie = { path = "../../crates/trie/trie" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1.52.3", default-features = false }
