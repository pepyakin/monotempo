# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/custom-payload-builder/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-custom-payload-builder"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-custom-payload-builder"
path = "src/main.rs"

[dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
eyre = { version = "^0.6" }
futures-util = { version = "^0.3", default-features = false }
reth-basic-payload-builder = { path = "../../crates/payload/basic" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "pool", "cli"] }
reth-ethereum-payload-builder = { path = "../../crates/ethereum/payload" }
reth-payload-builder = { path = "../../crates/payload/builder" }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
