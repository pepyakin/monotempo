# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/txpool-tracing/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-txpool-tracing"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-txpool-tracing"
path = "src/main.rs"

[dependencies]
alloy-network = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-trace = { version = "^2.4.2", default-features = false }
clap = { version = "^4", features = ["derive"] }
eyre = { version = "^0.6" }
futures-util = { version = "^0.3", default-features = false }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "pool", "cli", "rpc"] }
