# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/network-proxy/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-network-proxy"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-network-proxy"
path = "src/main.rs"

[dependencies]
eyre = { version = "^0.6" }
futures = { version = "^0.3" }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["network"] }
reth-metrics = { path = "../../crates/metrics" }
reth-tracing = { path = "../../crates/tracing" }
tokio = { version = "^1.52.3", default-features = false }
