# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/beacon-api-sse/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-beacon-api-sse"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-beacon-api-sse"
path = "src/main.rs"

[dependencies]
alloy-rpc-types-beacon = { version = "^2.4.2", default-features = false }
bytes = { version = "^1.11.1", default-features = false }
clap = { version = "^4" }
futures-util = { version = "^0.3", default-features = false }
reqwest = { version = "^0.13", features = ["rustls", "stream"], default-features = false }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "cli"] }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1.52.3", features = ["time"], default-features = false }
tracing = { version = "^0.1.0", features = ["attributes"], default-features = false }
