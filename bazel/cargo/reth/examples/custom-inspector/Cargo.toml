# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/custom-inspector/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-custom-inspector"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-custom-inspector"
path = "src/main.rs"

[dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-evm = { version = "^0.39.0", default-features = false }
alloy-primitives = { version = "^1.6.1", features = ["map-foldhash"], default-features = false }
alloy-rpc-types-eth = { version = "^2.4.2", default-features = false }
clap = { version = "^4", features = ["derive"] }
futures-util = { version = "^0.3", default-features = false }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "evm", "pool", "cli"] }
