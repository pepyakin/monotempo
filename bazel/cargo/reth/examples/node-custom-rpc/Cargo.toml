# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth/examples/node-custom-rpc/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-node-custom-rpc"
version = "0.0.0"
edition = "2024"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[[bin]]
name = "example-node-custom-rpc"
path = "src/main.rs"

[dependencies]
clap = { version = "^4", features = ["derive"] }
jsonrpsee = { version = "^0.26.0", features = ["server", "macros"] }
reth-ethereum = { path = "../../crates/ethereum/reth", features = ["node", "pool", "cli"] }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1.52.3", default-features = false }
