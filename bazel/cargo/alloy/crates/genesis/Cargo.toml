# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/genesis/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-genesis"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_genesis"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-primitives/std",
    "alloy-serde/std",
    "serde/std",
    "alloy-trie/std",
    "alloy-eips/std",
    "serde_json/std",
    "serde_with?/std",
    "borsh?/std",
]
serde-bincode-compat = ["dep:serde_with", "alloy-eips/serde-bincode-compat"]
borsh = ["dep:borsh", "alloy-primitives/borsh", "alloy-eips/borsh"]

[dependencies]
alloy-eips = { path = "../eips", features = ["serde"], default-features = false }
alloy-primitives = { version = "^1.6.0", default-features = false }
alloy-serde = { path = "../serde", default-features = false }
alloy-trie = { version = "^0.9.2", features = ["ethereum"], default-features = false }
borsh = { version = "^1.5", features = ["derive"], optional = true, default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }

[dev-dependencies]
bincode = { version = "^2.0", features = ["serde"] }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
