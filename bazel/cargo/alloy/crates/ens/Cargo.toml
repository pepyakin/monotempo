# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/ens/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-ens"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_ens"
path = "src/lib.rs"

[features]
contract = ["dep:alloy-contract", "dep:thiserror", "dep:alloy-sol-types"]
provider = ["contract", "dep:alloy-provider", "alloy-provider/ccip-read-http", "dep:async-trait"]
reqwest-default-tls = ["provider", "alloy-provider/reqwest-default-tls"]
reqwest-rustls-tls = ["provider", "alloy-provider/reqwest-rustls-tls"]
reqwest-native-tls = ["provider", "alloy-provider/reqwest-native-tls"]

[dependencies]
alloy-contract = { path = "../contract", optional = true, default-features = false }
alloy-primitives = { version = "^1.6.0", default-features = false }
alloy-provider = { path = "../provider", optional = true, default-features = false }
alloy-sol-types = { version = "^1.6.0", optional = true, default-features = false }
async-trait = { version = "^0.1", optional = true }
thiserror = { version = "^2.0", optional = true, default-features = false }

[dev-dependencies]
alloy-json-rpc = { path = "../json-rpc", default-features = false }
alloy-provider = { path = "../provider", features = ["reqwest", "reqwest-default-tls"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1", features = ["macros", "rt-multi-thread"] }
