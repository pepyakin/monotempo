# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-client/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-client"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_client"
path = "src/lib.rs"

[[test]]
name = "it"
path = "tests/it/main.rs"

[features]
default = ["reqwest"]
reqwest = ["dep:url", "dep:reqwest", "alloy-transport-http/reqwest"]
hyper = ["dep:url", "alloy-transport-http/hyper"]
pubsub = ["dep:alloy-pubsub"]
ws-base = ["pubsub", "dep:url", "dep:alloy-transport-ws"]
ws = ["ws-base", "alloy-transport-ws/aws-lc-rs"]
ws-ring = ["ws-base", "alloy-transport-ws/ring"]
ws-native-tls = ["ws-base", "alloy-transport-ws/native-tls"]
ipc = ["pubsub", "dep:alloy-transport-ipc"]

[dependencies]
alloy-json-rpc = { path = "../json-rpc", default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["map"], default-features = false }
alloy-pubsub = { path = "../pubsub", optional = true, default-features = false }
alloy-transport = { path = "../transport", default-features = false }
alloy-transport-http = { path = "../transport-http", optional = true, default-features = false }
alloy-transport-ws = { path = "../transport-ws", optional = true, default-features = false }
futures = { version = "^0.3" }
pin-project = { version = "^1.1" }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1", features = ["sync"] }
tokio-stream = { version = "^0.1", features = ["sync"], default-features = false }
tower = { version = "^0.5", features = ["util"] }
tracing = { version = "^0.1" }
url = { version = "^2.5", features = ["serde"], optional = true }

[dev-dependencies]
alloy-node-bindings = { path = "../node-bindings", default-features = false }
alloy-primitives = { version = "^1.6.0", default-features = false }
alloy-transport-ipc = { path = "../transport-ipc", features = ["mock"], default-features = false }
alloy-transport-ws = { path = "../transport-ws", features = ["aws-lc-rs"], default-features = false }
ci_info = { version = "^0.14.14" }
futures-util = { version = "^0.3" }
similar-asserts = { version = "^1.5" }
tempfile = { version = "^3" }

[target.'cfg(all(target_family = "wasm", target_os = "unknown"))'.dependencies]
wasmtimer = { version = "^0.4.0" }

[target.'cfg(not(all(target_os = "wasi", target_env = "p1")))'.dependencies]
reqwest = { version = "^0.13", optional = true, default-features = false }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
alloy-transport-ipc = { path = "../transport-ipc", optional = true, default-features = false }
