# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/signer-tempo/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-signer-tempo"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_signer_tempo"
path = "src/lib.rs"

[[test]]
name = "env"
path = "tests/env.rs"

[[test]]
name = "load"
path = "tests/load.rs"

[[test]]
name = "lookup"
path = "tests/lookup.rs"

[dependencies]
alloy-primitives = { version = "^1.6.0", features = ["serde"], default-features = false }
alloy-signer-local = { path = "../signer-local", default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
thiserror = { version = "^2.0", default-features = false }
toml = { version = "^0.8", features = ["parse"], default-features = false }
tracing = { version = "^0.1" }

[dev-dependencies]
tempfile = { version = "^3.20" }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
dirs = { version = "^5" }
