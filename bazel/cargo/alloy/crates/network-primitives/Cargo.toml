# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/network-primitives/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-network-primitives"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_network_primitives"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "alloy-consensus/std", "alloy-eips/std", "alloy-serde/std", "serde/std"]
serde = ["dep:serde", "alloy-consensus/serde", "alloy-eips/serde", "alloy-primitives/serde"]

[dependencies]
alloy-consensus = { path = "../consensus", features = ["serde"], default-features = false }
alloy-eips = { path = "../eips", default-features = false }
alloy-primitives = { version = "^1.6.0", default-features = false }
alloy-serde = { path = "../serde", default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], optional = true, default-features = false }
