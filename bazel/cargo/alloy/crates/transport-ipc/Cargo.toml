# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/transport-ipc/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-transport-ipc"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_transport_ipc"
path = "src/lib.rs"

[[test]]
name = "ipc"
path = "tests/ipc.rs"

[features]
default = []
mock = ["dep:tempfile"]

[dependencies]
alloy-json-rpc = { path = "../json-rpc", default-features = false }
alloy-pubsub = { path = "../pubsub", default-features = false }
alloy-transport = { path = "../transport", default-features = false }
bytes = { version = "^1.5.0" }
futures = { version = "^0.3" }
interprocess = { version = "^2", features = ["tokio"] }
memchr = { version = "^2.7" }
pin-project = { version = "^1.1" }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tempfile = { version = "^3.20", optional = true }
tokio = { version = "^1" }
tokio-util = { version = "^0.7", features = ["io"] }
tracing = { version = "^0.1" }

[dev-dependencies]
tempfile = { version = "^3.20" }
tokio = { version = "^1", features = ["macros", "rt-multi-thread", "io-util", "time"] }
tokio-test = { version = "^0.4" }
