# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/serde/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-serde"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_serde"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "serde/std", "serde_json/std"]
arbitrary = ["dep:arbitrary", "alloy-primitives/arbitrary", "std"]

[dependencies]
alloy-primitives = { version = "^1.6.0", features = ["serde", "map"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc", "alloc"], default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.0", features = ["rand", "rlp", "serde", "arbitrary"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
rand = { version = "^0.8" }
similar-asserts = { version = "^1.5" }
