# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-types-trace/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-types-trace"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_types_trace"
path = "src/lib.rs"

[dependencies]
alloy-primitives = { version = "^1.6.0", features = ["rlp", "serde"], default-features = false }
alloy-rpc-types-eth = { path = "../rpc-types-eth", features = ["std", "serde"], default-features = false }
alloy-serde = { path = "../serde", default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
thiserror = { version = "^2.0", default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.6.0", features = ["rand", "rlp", "serde", "arbitrary"], default-features = false }
similar-asserts = { version = "^1.5" }
