# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/signer/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-signer"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_signer"
path = "src/lib.rs"

[features]
eip712 = ["dep:alloy-sol-types", "dep:alloy-dyn-abi", "alloy-dyn-abi?/eip712"]

[dependencies]
alloy-dyn-abi = { version = "^1.6.0", features = ["std", "eip712"], optional = true, default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["std", "k256"], default-features = false }
alloy-sol-types = { version = "^1.6.0", features = ["std"], optional = true, default-features = false }
async-trait = { version = "^0.1" }
auto_impl = { version = "^1.2" }
either = { version = "^1.15", default-features = false }
elliptic-curve = { version = "^0.13", default-features = false }
k256 = { version = "^0.13", features = ["ecdsa"], default-features = false }
thiserror = { version = "^2.0", default-features = false }

[dev-dependencies]
assert_matches = { version = "^1.5" }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
tokio = { version = "^1", features = ["macros", "rt-multi-thread"] }
