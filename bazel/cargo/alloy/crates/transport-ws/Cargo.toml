# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/transport-ws/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-transport-ws"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_transport_ws"
path = "src/lib.rs"

[features]
default = ["aws-lc-rs"]
aws-lc-rs = ["rustls-tls", "rustls?/aws-lc-rs"]
ring = ["rustls-tls", "rustls?/ring"]
native-tls = ["tokio-tungstenite/native-tls"]
rustls-tls = ["dep:rustls", "tokio-tungstenite/rustls-tls-webpki-roots"]

[dependencies]
alloy-pubsub = { path = "../pubsub", default-features = false }
alloy-transport = { path = "../transport", default-features = false }
futures = { version = "^0.3" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
tokio = { version = "^1", features = ["sync", "rt"] }
tracing = { version = "^0.1" }
url = { version = "^2.5" }

[target.'cfg(not(target_family = "wasm"))'.dependencies]
http = { version = "^1.1" }
rustls = { version = "^0.23", optional = true, default-features = false }
tokio = { version = "^1", features = ["sync", "rt", "time"] }
tokio-tungstenite = { version = "^0.29" }

[target.'cfg(target_family = "wasm")'.dependencies]
ws_stream_wasm = { version = "^0.7.4" }
