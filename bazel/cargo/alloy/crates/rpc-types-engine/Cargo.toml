# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-types-engine/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-types-engine"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_types_engine"
path = "src/lib.rs"

[features]
default = ["std", "serde"]
std = [
    "alloy-consensus/std",
    "derive_more/std",
    "strum/std",
    "alloy-eips/std",
    "alloy-serde?/std",
    "alloy-primitives/std",
    "alloy-rlp/std",
    "serde?/std",
]
serde = [
    "dep:serde",
    "dep:alloy-serde",
    "alloy-consensus/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "rand?/serde",
]
jwt = ["std", "dep:jsonwebtoken", "dep:rand"]
jwt-aws-lc-rs = ["jwt", "jsonwebtoken/aws_lc_rs"]
ssz = ["std", "dep:ethereum_ssz", "dep:ethereum_ssz_derive", "alloy-eips/ssz"]
kzg = ["alloy-consensus/kzg", "alloy-eips/kzg"]
arbitrary = [
    "dep:arbitrary",
    "std",
    "alloy-primitives/arbitrary",
    "alloy-serde/arbitrary",
    "alloy-consensus/arbitrary",
    "alloy-eips/arbitrary",
]

[dependencies]
alloy-consensus = { path = "../consensus", features = ["serde"], default-features = false }
alloy-eips = { path = "../eips", features = ["serde"], default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["rlp", "serde"], default-features = false }
alloy-rlp = { version = "^0.3.14", features = ["arrayvec", "derive"], default-features = false }
alloy-serde = { path = "../serde", optional = true, default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
derive_more = { version = "^2", features = ["display"], default-features = false }
ethereum_ssz = { version = "^0.10", optional = true }
ethereum_ssz_derive = { version = "^0.10", optional = true }
jsonwebtoken = { version = "^10.3.0", optional = true }
rand = { version = "^0.8", optional = true }
serde = { version = "^1.0.226", features = ["derive", "alloc", "derive"], optional = true, default-features = false }
strum = { version = "^0.27", features = ["derive"], default-features = false }

[dev-dependencies]
alloy-consensus = { path = "../consensus", features = ["arbitrary"], default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["rand", "rlp", "serde", "arbitrary"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
assert_matches = { version = "^1.5" }
base64 = { version = "^0.22" }
jsonwebtoken = { version = "^10.3.0", features = ["aws_lc_rs"] }
rand = { version = "^0.8" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
similar-asserts = { version = "^1.5" }
tempfile = { version = "^3.20" }
