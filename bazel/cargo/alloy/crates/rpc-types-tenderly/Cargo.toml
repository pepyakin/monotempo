# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-types-tenderly/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-types-tenderly"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_types_tenderly"
path = "src/lib.rs"

[dependencies]
alloy-consensus = { path = "../consensus", features = ["serde"], default-features = false }
alloy-dyn-abi = { version = "^1.6.0", default-features = false }
alloy-eips = { path = "../eips", features = ["serde"], default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["serde"], default-features = false }
alloy-serde = { path = "../serde", default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
