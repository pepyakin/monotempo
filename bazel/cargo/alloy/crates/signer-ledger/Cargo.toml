# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/signer-ledger/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-signer-ledger"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_signer_ledger"
path = "src/lib.rs"

[features]
eip712 = ["alloy-signer/eip712", "dep:alloy-sol-types", "dep:alloy-dyn-abi", "alloy-dyn-abi?/eip712"]
eip7702 = ["alloy-eip7702"]
browser = ["coins-ledger/browser"]
node = ["coins-ledger/node"]

[dependencies]
alloy-consensus = { path = "../consensus", features = ["std"], default-features = false }
alloy-dyn-abi = { version = "^1.6.0", optional = true, default-features = false }
alloy-eip7702 = { version = "^0.6.3", optional = true, default-features = false }
alloy-network = { path = "../network", default-features = false }
alloy-primitives = { version = "^1.6.0", default-features = false }
alloy-signer = { path = "../signer", default-features = false }
alloy-sol-types = { version = "^1.6.0", optional = true, default-features = false }
async-trait = { version = "^0.1" }
coins-ledger = { version = "^0.13", default-features = false }
futures-util = { version = "^0.3" }
semver = { version = "^1.0" }
thiserror = { version = "^2.0", default-features = false }
tracing = { version = "^0.1" }

[dev-dependencies]
alloy-consensus = { path = "../consensus", features = ["std"], default-features = false }
alloy-rlp = { version = "^0.3.14", default-features = false }
tokio = { version = "^1", features = ["macros", "rt-multi-thread"] }
tracing-subscriber = { version = "^0.3" }
