# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-types-eth/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-types-eth"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_types_eth"
path = "src/lib.rs"

[features]
default = ["std", "serde"]
std = [
    "alloy-primitives/std",
    "alloy-consensus/std",
    "alloy-eips/std",
    "alloy-consensus-any/std",
    "alloy-network-primitives/std",
    "alloy-serde?/std",
    "alloy-rlp/std",
    "alloy-sol-types/std",
    "serde?/std",
    "serde_json?/std",
    "serde_with?/std",
    "thiserror/std",
]
serde = [
    "dep:serde",
    "dep:serde_json",
    "dep:alloy-serde",
    "alloy-primitives/serde",
    "alloy-consensus/serde",
    "alloy-eips/serde",
    "alloy-consensus-any/serde",
    "rand/serde",
    "alloy-network-primitives/serde",
]
arbitrary = [
    "std",
    "dep:arbitrary",
    "alloy-consensus/arbitrary",
    "alloy-primitives/arbitrary",
    "alloy-serde?/arbitrary",
    "alloy-eips/arbitrary",
    "alloy-consensus-any/arbitrary",
    "alloy-sol-types/arbitrary",
]
k256 = ["alloy-consensus/k256", "alloy-eips/k256", "alloy-primitives/k256"]
serde-bincode-compat = [
    "serde",
    "dep:serde_with",
    "alloy-eips/serde-bincode-compat",
    "alloy-consensus/serde-bincode-compat",
]

[dependencies]
alloy-consensus = { path = "../consensus", default-features = false }
alloy-consensus-any = { path = "../consensus-any", default-features = false }
alloy-eips = { path = "../eips", default-features = false }
alloy-network-primitives = { path = "../network-primitives", default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["rlp", "map"], default-features = false }
alloy-rlp = { version = "^0.3.14", features = ["arrayvec", "derive"], default-features = false }
alloy-serde = { path = "../serde", optional = true, default-features = false }
alloy-sol-types = { version = "^1.6.0", default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
itertools = { version = ">=0.13, <=0.14", default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc", "derive"], optional = true, default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], optional = true, default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }
thiserror = { version = "^2.0", default-features = false }

[dev-dependencies]
alloy-consensus = { path = "../consensus", features = ["std", "arbitrary"], default-features = false }
alloy-eips = { path = "../eips", features = ["arbitrary", "k256"], default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["rand", "rlp", "serde", "arbitrary"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
assert_matches = { version = "^1.5" }
bincode = { version = "^2.0", features = ["serde"] }
rand = { version = "^0.8" }
similar-asserts = { version = "^1.5" }
