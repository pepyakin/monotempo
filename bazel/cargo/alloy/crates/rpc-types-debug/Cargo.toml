# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/rpc-types-debug/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-rpc-types-debug"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_rpc_types_debug"
path = "src/lib.rs"

[dependencies]
alloy-primitives = { version = "^1.6.0", features = ["serde"], default-features = false }
alloy-rlp = { version = "^0.3.14", default-features = false }
derive_more = { version = "^2", features = ["as_ref", "deref", "deref_mut", "from"], default-features = false }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_with = { version = "^3", features = ["macros", "alloc", "base64"], default-features = false }

[dev-dependencies]
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
