# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy/crates/network/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-network"
version = "2.4.2"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_network"
path = "src/lib.rs"

[features]
k256 = ["alloy-primitives/k256", "alloy-consensus/k256", "alloy-eips/k256", "alloy-rpc-types-eth/k256"]

[dependencies]
alloy-consensus = { path = "../consensus", features = ["std"], default-features = false }
alloy-consensus-any = { path = "../consensus-any", features = ["std", "serde"], default-features = false }
alloy-eips = { path = "../eips", features = ["serde"], default-features = false }
alloy-json-rpc = { path = "../json-rpc", default-features = false }
alloy-network-primitives = { path = "../network-primitives", default-features = false }
alloy-primitives = { version = "^1.6.0", features = ["map"], default-features = false }
alloy-rpc-types-any = { path = "../rpc-types-any", default-features = false }
alloy-rpc-types-eth = { path = "../rpc-types-eth", features = ["std", "serde"], default-features = false }
alloy-serde = { path = "../serde", default-features = false }
alloy-signer = { path = "../signer", default-features = false }
alloy-sol-types = { version = "^1.6.0", default-features = false }
async-trait = { version = "^0.1" }
auto_impl = { version = "^1.2" }
derive_more = { version = "^2", features = ["from"], default-features = false }
futures-utils-wasm = { version = "^0.1" }
serde = { version = "^1.0.226", features = ["derive", "alloc"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
thiserror = { version = "^2.0", default-features = false }
