# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/examples/database_components/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-database-components"
version = "0.0.0"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "example_database_components"
path = "src/lib.rs"

[dependencies]
auto_impl = { version = "^1.3.0" }
revm = { path = "../../crates/revm", default-features = false }
thiserror = { version = "^2.0", default-features = false }
