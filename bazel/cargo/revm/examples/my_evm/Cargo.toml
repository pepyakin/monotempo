# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/examples/my_evm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "example-my-evm"
version = "0.0.0"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "example_my_evm"
path = "src/lib.rs"

[[bin]]
name = "example-my-evm"
path = "src/main.rs"

[features]
asyncdb = ["revm/asyncdb"]

[dependencies]
revm = { path = "../../crates/revm", default-features = false }
