# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/statetest-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-statetest-types"
version = "43.0.1"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_statetest_types"
path = "src/lib.rs"

[features]
map-foldhash = ["primitives/map-foldhash", "state/map-foldhash", "context/map-foldhash", "database/map-foldhash"]

[dependencies]
alloy-eip7928 = { version = "^0.4.0", features = ["std", "serde", "rlp"], default-features = false }
context = { package = "revm-context", path = "../context", features = ["std", "serde"], default-features = false }
context-interface = { package = "revm-context-interface", path = "../context/interface", features = ["std", "serde"], default-features = false }
database = { package = "revm-database", path = "../database", features = ["std", "serde"], default-features = false }
k256 = { version = "^0.13.4", default-features = false }
primitives = { package = "revm-primitives", path = "../primitives", features = ["std", "serde"], default-features = false }
serde = { version = "^1.0", features = ["derive", "rc", "derive", "rc"], default-features = false }
serde_json = { version = "^1.0", features = ["preserve_order"], default-features = false }
state = { package = "revm-state", path = "../state", features = ["std", "serde"], default-features = false }
thiserror = { version = "^2.0", default-features = false }
