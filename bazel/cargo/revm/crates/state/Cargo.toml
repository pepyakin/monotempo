# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/state/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-state"
version = "43.0.0"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_state"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "serde?/std",
    "serde_json/std",
    "primitives/std",
    "bitflags/std",
    "bytecode/std",
    "alloy-eip7928/std",
    "nonmax/std",
]
serde = [
    "dep:serde",
    "primitives/serde",
    "bitflags/serde",
    "bytecode/serde",
    "alloy-eip7928/serde",
    "nonmax/serde",
]
map-foldhash = ["primitives/map-foldhash"]

[dependencies]
alloy-eip7928 = { version = "^0.4.0", default-features = false }
bitflags = { version = "^2.10", default-features = false }
bytecode = { package = "revm-bytecode", path = "../bytecode", default-features = false }
nonmax = { version = "^0.5.5", default-features = false }
primitives = { package = "revm-primitives", path = "../primitives", default-features = false }
serde = { version = "^1.0", features = ["derive", "rc", "derive", "rc"], optional = true, default-features = false }

[dev-dependencies]
postcard = { version = "^1.1", features = ["use-std"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
