# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/context/interface/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-context-interface"
version = "43.0.1"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_context_interface"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "serde?/std",
    "alloy-eip7702/std",
    "alloy-eip2930/std",
    "database-interface/std",
    "primitives/std",
    "state/std",
    "either/std",
]
serde = [
    "dep:serde",
    "primitives/serde",
    "state/serde",
    "alloy-eip7702/serde",
    "alloy-eip2930/serde",
    "database-interface/serde",
    "either/serde",
]
serde-json = ["serde"]

[dependencies]
alloy-eip2930 = { version = "^0.2.3", default-features = false }
alloy-eip7702 = { version = "^0.6.3", features = ["k256"], default-features = false }
auto_impl = { version = "^1.3.0" }
database-interface = { package = "revm-database-interface", path = "../../database/interface", default-features = false }
either = { version = "^1.15.0", default-features = false }
primitives = { package = "revm-primitives", path = "../../primitives", default-features = false }
serde = { version = "^1.0", features = ["derive", "rc"], optional = true, default-features = false }
state = { package = "revm-state", path = "../../state", default-features = false }
