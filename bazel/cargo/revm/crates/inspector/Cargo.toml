# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/inspector/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-inspector"
version = "43.0.2"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_inspector"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "serde?/std",
    "serde_json?/std",
    "serde_json?/preserve_order",
    "context/std",
    "database/std",
    "database-interface/std",
    "handler/std",
    "interpreter/std",
    "primitives/std",
    "state/std",
    "either/std",
]
serde = [
    "dep:serde",
    "context/serde",
    "database/serde",
    "database-interface/serde",
    "handler/serde",
    "interpreter/serde",
    "primitives/serde",
    "state/serde",
    "either/serde",
]
tracer = ["std", "serde", "dep:serde_json"]
serde-json = ["tracer"]

[dependencies]
auto_impl = { version = "^1.3.0" }
context = { package = "revm-context", path = "../context", default-features = false }
database-interface = { package = "revm-database-interface", path = "../database/interface", default-features = false }
either = { version = "^1.15.0", default-features = false }
handler = { package = "revm-handler", path = "../handler", default-features = false }
interpreter = { package = "revm-interpreter", path = "../interpreter", default-features = false }
primitives = { package = "revm-primitives", path = "../primitives", default-features = false }
serde = { version = "^1.0", features = ["derive", "rc", "derive", "rc"], optional = true, default-features = false }
serde_json = { version = "^1.0", features = ["alloc", "preserve_order"], optional = true, default-features = false }
state = { package = "revm-state", path = "../state", default-features = false }

[dev-dependencies]
database = { package = "revm-database", path = "../database", features = ["serde"], default-features = false }
