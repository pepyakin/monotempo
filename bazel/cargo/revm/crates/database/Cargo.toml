# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/database/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-database"
version = "43.0.0"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_database"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "serde?/std",
    "alloy-eips?/std",
    "bytecode/std",
    "database-interface/std",
    "derive_more/std",
    "either/std",
    "primitives/std",
    "state/std",
    "serde_json/std",
]
serde = [
    "dep:serde",
    "alloy-eips?/serde",
    "bytecode/serde",
    "database-interface/serde",
    "primitives/serde",
    "state/serde",
    "either/serde",
]
alloydb = [
    "std",
    "database-interface/asyncdb",
    "dep:tokio",
    "dep:alloy-provider",
    "dep:alloy-eips",
    "dep:alloy-transport",
]
map-foldhash = ["primitives/map-foldhash", "state/map-foldhash"]

[dependencies]
alloy-eips = { version = "^2.0.0", optional = true, default-features = false }
alloy-provider = { version = "^2.0.0", optional = true, default-features = false }
alloy-transport = { version = "^2.0.0", optional = true, default-features = false }
bytecode = { package = "revm-bytecode", path = "../bytecode", default-features = false }
database-interface = { package = "revm-database-interface", path = "interface", default-features = false }
derive_more = { version = "^2.0", features = ["debug"], default-features = false }
either = { version = "^1.15.0", default-features = false }
primitives = { package = "revm-primitives", path = "../primitives", default-features = false }
serde = { version = "^1.0", features = ["derive", "rc", "derive", "rc"], optional = true, default-features = false }
state = { package = "revm-state", path = "../state", default-features = false }
tokio = { version = "^1.47", features = ["rt-multi-thread", "macros"], optional = true }

[dev-dependencies]
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
