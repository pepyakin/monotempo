# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `revm/crates/database/interface/Cargo.toml`: the same
# package as `cargo metadata` describes it in the revm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "revm-database-interface"
version = "43.0.0"
edition = "2021"
rust-version = "1.91.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "revm_database_interface"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["serde?/std", "primitives/std", "state/std", "either/std", "thiserror/std"]
serde = ["dep:serde", "primitives/serde", "state/serde", "either/serde"]
asyncdb = ["std", "dep:tokio", "tokio/rt-multi-thread", "dep:corosensei"]

[dependencies]
auto_impl = { version = "^1.3.0" }
corosensei = { version = "^0.3.4", features = ["default-stack", "unwind"], optional = true, default-features = false }
either = { version = "^1.15.0", default-features = false }
primitives = { package = "revm-primitives", path = "../../primitives", default-features = false }
serde = { version = "^1.0", features = ["derive", "rc", "derive", "rc"], optional = true, default-features = false }
state = { package = "revm-state", path = "../../state", default-features = false }
thiserror = { version = "^2.0", default-features = false }
tokio = { version = "^1.47", optional = true }
