# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-trie/./Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-trie workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-trie"
version = "0.9.5"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_trie"
path = "src/lib.rs"

[[bench]]
name = "bench"
path = "benches/bench.rs"
required-features = ["arbitrary"]

[features]
default = ["std", "alloy-primitives/default"]
std = [
    "alloy-primitives/std",
    "alloy-rlp/std",
    "derive_more/std",
    "nybbles/std",
    "thiserror/std",
    "tracing/std",
    "serde?/std",
]
serde = ["dep:serde", "alloy-primitives/serde", "nybbles/serde"]
arbitrary = [
    "std",
    "dep:arbitrary",
    "dep:derive_arbitrary",
    "dep:proptest",
    "dep:proptest-derive",
    "alloy-primitives/arbitrary",
    "nybbles/arbitrary",
]
ethereum = []

[dependencies]
alloy-primitives = { version = "^1.0", features = ["rlp", "map"], default-features = false }
alloy-rlp = { version = "^0.3.9", features = ["derive", "arrayvec", "core-net", "core-error"], default-features = false }
arbitrary = { version = "^1.3", optional = true }
derive_arbitrary = { version = "^1.3", optional = true }
derive_more = { version = "^2", features = ["add", "add_assign", "deref", "from", "not"], default-features = false }
nybbles = { version = "^0.4", default-features = false }
proptest = { version = "^1.5", optional = true }
proptest-derive = { version = "^0.7", optional = true }
serde = { version = "^1.0", features = ["derive", "rc"], optional = true, default-features = false }
smallvec = { version = "^1.0", features = ["const_new"], default-features = false }
thiserror = { version = "^2", default-features = false }
tracing = { version = "^0.1", default-features = false }

[dev-dependencies]
codspeed-criterion-compat = { version = "^4.3" }
hash-db = { version = "^0.15" }
plain_hasher = { version = "^0.2" }
triehash = { version = "^0.8.4" }
