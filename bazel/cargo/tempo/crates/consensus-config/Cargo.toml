# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/consensus-config/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-consensus-config"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_consensus_config"
path = "src/lib.rs"

[dependencies]
age = { version = "^0.11", default-features = false }
commonware-codec = { version = "^2026.9.0" }
commonware-cryptography = { version = "^2026.9.0", features = ["bls12381"], default-features = false }
commonware-math = { version = "^2026.9.0" }
const-hex = { version = "^1.17.0" }
rand_core = { version = "^0.10" }
secrecy = { version = "^0.10" }
thiserror = { version = "^2.0.18" }

[dev-dependencies]
commonware-utils = { version = "^2026.9.0" }
rand = { version = "^0.10" }
tempfile = { version = "^3.24.0" }
