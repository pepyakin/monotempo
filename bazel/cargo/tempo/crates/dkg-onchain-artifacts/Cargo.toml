# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/dkg-onchain-artifacts/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-dkg-onchain-artifacts"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_dkg_onchain_artifacts"
path = "src/lib.rs"

[dependencies]
bytes = { version = "^1.11.1" }
commonware-codec = { version = "^2026.9.0" }
commonware-consensus = { version = "^2026.9.0", optional = true }
commonware-cryptography = { version = "^2026.9.0", features = ["bls12381", "std"], default-features = false }
commonware-utils = { version = "^2026.9.0" }
modular-bitfield = { version = "^0.13.1", optional = true }

[dev-dependencies]
commonware-consensus = { version = "^2026.9.0" }
commonware-math = { version = "^2026.9.0" }
rand = { version = "^0.10" }
