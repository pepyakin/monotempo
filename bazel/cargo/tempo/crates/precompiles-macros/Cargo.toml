# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/precompiles-macros/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-precompiles-macros"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_precompiles_macros"
path = "src/lib.rs"
proc-macro = true

[dependencies]
alloy-primitives = { version = "^1.7.3", default-features = false }
proc-macro2 = { version = "^1" }
quote = { version = "^1" }
syn = { version = "^2", features = ["full", "extra-traits"] }

[dev-dependencies]
alloy = { version = "^2.4.2", default-features = false }
