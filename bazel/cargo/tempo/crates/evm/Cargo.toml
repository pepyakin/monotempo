# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/evm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-evm"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_evm"
path = "src/lib.rs"

[[test]]
name = "reexports"
path = "tests/reexports.rs"

[[bench]]
name = "dex_execution"
path = "benches/dex_execution.rs"

[[bench]]
name = "tip20_execution"
path = "benches/tip20_execution.rs"

[features]
default = ["rpc", "engine"]
rpc = [
    "dep:alloy-rpc-types-eth",
    "dep:reth-rpc-eth-api",
    "tempo-primitives/serde",
    "tempo-primitives/reth-codec",
]
engine = ["dep:tempo-payload-types", "dep:rayon"]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-evm = { version = "^0.39.0", features = ["std"], default-features = false }
alloy-primitives = { version = "^1.7.3", default-features = false }
alloy-rlp = { version = "^0.3.16", default-features = false }
alloy-rpc-types-eth = { version = "^2.4.2", optional = true }
alloy-sol-types = { version = "^1.7.3", default-features = false }
commonware-codec = { version = "^2026.9.0" }
derive_more = { version = "^2.1.1", default-features = false }
rayon = { version = "^1.11", optional = true }
reth-chainspec = { path = "../../../reth/crates/chainspec", default-features = false }
reth-consensus = { path = "../../../reth/crates/consensus/consensus" }
reth-consensus-common = { path = "../../../reth/crates/consensus/common" }
reth-ethereum-consensus = { path = "../../../reth/crates/ethereum/consensus" }
reth-evm = { path = "../../../reth/crates/evm/evm" }
reth-evm-ethereum = { path = "../../../reth/crates/ethereum/evm" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-revm = { path = "../../../reth/crates/revm", features = ["std", "optional-checks"] }
reth-rpc-eth-api = { path = "../../../reth/crates/rpc/rpc-eth-api", optional = true }
tempo-chainspec = { path = "../chainspec", features = ["reth"], default-features = false }
tempo-contracts = { path = "../contracts", features = ["serde"], default-features = false }
tempo-dkg-onchain-artifacts = { path = "../dkg-onchain-artifacts", default-features = false }
tempo-payload-types = { path = "../payload/types", optional = true, default-features = false }
tempo-precompiles = { path = "../precompiles", default-features = false }
tempo-primitives = { path = "../primitives", features = ["reth"], default-features = false }
tempo-revm = { path = "../revm", default-features = false }
thiserror = { version = "^2.0.18" }
tracing = { version = "^0.1.44" }

[dev-dependencies]
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-genesis = { version = "^2.4.2", features = ["std"], default-features = false }
alloy-primitives = { version = "^1.7.3", features = ["asm-keccak", "keccak-cache-global"], default-features = false }
alloy-signer = { version = "^2.4.2" }
alloy-signer-local = { version = "^2.4.2", features = ["mnemonic"] }
alloy-sol-types = { version = "^1.7.3", default-features = false }
commonware-cryptography = { version = "^2026.9.0", default-features = false }
commonware-math = { version = "^2026.9.0" }
commonware-utils = { version = "^2026.9.0" }
codspeed-criterion-compat = { version = "^4.6.0" }
eyre = { version = "^0.6.12" }
indexmap = { version = "^2.13.0" }
insta = { version = "^1", features = ["yaml"] }
rand = { version = "^0.10" }
reth-execution-cache = { path = "../../../reth/crates/engine/execution-cache" }
reth-storage-api = { path = "../../../reth/crates/storage/storage-api" }
reth-trie = { path = "../../../reth/crates/trie/trie" }
revm = { version = "^43.0.2", features = ["optional_fee_charge"], default-features = false }
serde_json = { version = "^1.0.149", features = ["alloc", "raw_value"], default-features = false }
tempo-precompiles = { path = "../precompiles", features = ["test-utils"], default-features = false }
tempo-primitives = { path = "../primitives", features = ["arbitrary", "test-utils"], default-features = false }
tikv-jemallocator = { version = "^0.6.1" }
