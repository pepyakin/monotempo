# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/faucet/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-faucet"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_faucet"
path = "src/lib.rs"

[dependencies]
alloy = { version = "^2.4.2", features = ["rpc-types", "signers", "signer-local", "signer-mnemonic-all-languages", "providers", "reqwest-rustls-tls"], default-features = false }
async-trait = { version = "^0.1" }
clap = { version = "^4.5.57", features = ["derive", "env"] }
jsonrpsee = { version = "^0.26.0", features = ["server", "client", "macros"] }
reth-rpc-server-types = { path = "../../../reth/crates/rpc/rpc-server-types" }
tempo-alloy = { path = "../alloy", features = ["reth"] }
tempo-precompiles = { path = "../precompiles", features = ["rpc"], default-features = false }
