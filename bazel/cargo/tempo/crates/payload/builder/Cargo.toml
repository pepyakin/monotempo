# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `tempo/crates/payload/builder/Cargo.toml`: the same
# package as `cargo metadata` describes it in the tempo workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "tempo-payload-builder"
version = "1.14.0"
edition = "2024"
rust-version = "1.95.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "tempo_payload_builder"
path = "src/lib.rs"

[features]
bal = []
trie-debug = ["reth-engine-tree/trie-debug"]

[dependencies]
alloy-consensus = { version = "^2.4.2", default-features = false }
alloy-eip7928 = { version = "^0.4.9", default-features = false }
alloy-eips = { version = "^2.4.2", default-features = false }
alloy-primitives = { version = "^1.7.3", default-features = false }
alloy-rlp = { version = "^0.3.16", default-features = false }
crossbeam-channel = { version = "^0.5" }
metrics = { version = "^0.24.3" }
reth-basic-payload-builder = { path = "../../../../reth/crates/payload/basic" }
reth-chainspec = { path = "../../../../reth/crates/chainspec", default-features = false }
reth-consensus-common = { path = "../../../../reth/crates/consensus/common" }
reth-engine-tree = { path = "../../../../reth/crates/engine/tree" }
reth-errors = { path = "../../../../reth/crates/errors" }
reth-evm = { path = "../../../../reth/crates/evm/evm" }
reth-execution-types = { path = "../../../../reth/crates/evm/execution-types" }
reth-metrics = { path = "../../../../reth/crates/metrics" }
reth-payload-builder = { path = "../../../../reth/crates/payload/builder" }
reth-payload-primitives = { path = "../../../../reth/crates/payload/primitives" }
reth-primitives-traits = { version = "^0.7.1", default-features = false }
reth-revm = { path = "../../../../reth/crates/revm", features = ["std", "optional-checks"] }
reth-storage-api = { path = "../../../../reth/crates/storage/storage-api" }
reth-tasks = { path = "../../../../reth/crates/tasks" }
reth-transaction-pool = { path = "../../../../reth/crates/transaction-pool" }
reth-trie-common = { path = "../../../../reth/crates/trie/common" }
tempo-chainspec = { path = "../../chainspec", features = ["reth"], default-features = false }
tempo-evm = { path = "../../evm", default-features = false }
tempo-payload-types = { path = "../types", default-features = false }
tempo-precompiles = { path = "../../precompiles", default-features = false }
tempo-primitives = { path = "../../primitives", features = ["reth"], default-features = false }
tempo-transaction-pool = { path = "../../transaction-pool", default-features = false }
tokio = { version = "^1.49.0", features = ["full"] }
tracing = { version = "^0.1.44" }

[dev-dependencies]
proptest = { version = "^1.9" }
