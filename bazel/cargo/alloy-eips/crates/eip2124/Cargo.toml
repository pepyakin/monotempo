# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-eips/crates/eip2124/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-eips workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-eip2124"
version = "0.2.0"
edition = "2024"
rust-version = "1.90"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_eip2124"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "alloy-rlp/std", "serde?/std", "thiserror/std"]
serde = ["dep:serde", "alloy-primitives/serde"]
arbitrary = ["std", "dep:arbitrary", "dep:rand", "alloy-primitives/arbitrary"]

[dependencies]
alloy-primitives = { version = "^1.6", features = ["rlp"], default-features = false }
alloy-rlp = { version = "^0.3", features = ["derive"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
crc = { version = "^3" }
rand = { version = "^0.8", optional = true }
serde = { version = "^1.0", features = ["derive", "alloc"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
alloy-rlp = { version = "^0.3", features = ["arrayvec"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
