# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-eips/crates/eip7928/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-eips workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-eip7928"
version = "0.4.10"
edition = "2024"
rust-version = "1.90"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_eip7928"
path = "src/lib.rs"

[features]
default = ["std"]
std = [
    "alloy-primitives/std",
    "alloy-rlp/std",
    "once_cell/std",
    "serde?/std",
    "thiserror/std",
    "borsh?/std",
]
borsh = ["dep:borsh", "alloy-primitives/borsh"]
rlp = ["alloy-primitives/rlp"]
serde = ["dep:serde", "alloy-primitives/serde"]
arbitrary = ["std", "dep:arbitrary", "alloy-primitives/arbitrary"]

[dependencies]
alloy-primitives = { version = "^1.6", features = ["map"], default-features = false }
alloy-rlp = { version = "^0.3", features = ["derive"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
borsh = { version = "^1.5", features = ["derive"], optional = true, default-features = false }
once_cell = { version = "^1.20", features = ["alloc", "race"], default-features = false }
rand = { version = "^0.8", optional = true }
serde = { version = "^1.0", features = ["derive", "alloc"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
