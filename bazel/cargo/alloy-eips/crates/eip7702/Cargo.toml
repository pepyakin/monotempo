# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-eips/crates/eip7702/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-eips workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-eip7702"
version = "0.6.3"
edition = "2024"
rust-version = "1.90"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_eip7702"
path = "src/lib.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "alloy-rlp/std", "serde?/std", "thiserror/std", "borsh?/std"]
serde = ["dep:serde", "alloy-primitives/serde"]
serde-bincode-compat = ["serde_with"]
arbitrary = ["std", "dep:arbitrary", "dep:rand", "alloy-primitives/arbitrary"]
k256 = ["alloy-primitives/k256", "dep:k256"]
secp256k1 = ["alloy-primitives/secp256k1"]
borsh = ["dep:borsh", "alloy-primitives/borsh"]

[dependencies]
alloy-primitives = { version = "^1.6", features = ["rlp"], default-features = false }
alloy-rlp = { version = "^0.3", features = ["derive"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
borsh = { version = "^1.5", features = ["derive"], optional = true, default-features = false }
k256 = { version = "^0.13", features = ["ecdsa"], optional = true, default-features = false }
rand = { version = "^0.8", optional = true }
serde = { version = "^1.0", features = ["derive", "alloc"], optional = true, default-features = false }
serde_with = { version = "^3", features = ["macros"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
bincode = { version = "^1.3" }
rand = { version = "^0.8" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
