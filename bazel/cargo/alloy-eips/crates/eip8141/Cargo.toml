# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-eips/crates/eip8141/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-eips workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-eip8141"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_eip8141"
path = "src/lib.rs"

[[test]]
name = "encoding"
path = "tests/encoding.rs"

[[test]]
name = "serde"
path = "tests/serde.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "alloy-rlp/std", "serde?/std", "borsh?/std", "thiserror/std"]
serde = ["dep:serde", "alloy-primitives/serde"]
arbitrary = ["std", "dep:arbitrary", "alloy-primitives/arbitrary"]
borsh = ["dep:borsh", "alloy-primitives/borsh"]

[dependencies]
alloy-primitives = { version = "^1.6", features = ["rlp"], default-features = false }
alloy-rlp = { version = "^0.3", features = ["derive"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
borsh = { version = "^1.5", features = ["derive"], optional = true, default-features = false }
serde = { version = "^1.0", features = ["derive", "alloc"], optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
bincode = { version = "^1.3" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
