# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-evm/crates/evm/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-evm workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-evm"
version = "0.39.0"
edition = "2021"
rust-version = "1.94.1"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_evm"
path = "src/lib.rs"

[features]
default = ["std"]
secp256k1 = ["std", "alloy-consensus/secp256k1"]
std = [
    "alloy-primitives/std",
    "revm/std",
    "alloy-consensus/std",
    "alloy-eips/std",
    "alloy-sol-types/std",
    "derive_more/std",
    "thiserror/std",
    "alloy-rpc-types-eth?/std",
    "alloy-rpc-types-engine?/std",
    "tracing/std",
]
gmp = ["revm/gmp"]
overrides = ["dep:alloy-rpc-types-eth"]
call-util = ["overrides"]
engine = ["dep:alloy-rpc-types-engine"]
asm-keccak = ["alloy-primitives/asm-keccak", "revm/asm-keccak"]
rpc = ["dep:alloy-rpc-types-eth"]

[dependencies]
alloy-consensus = { version = "^2.0.0", features = ["k256"], default-features = false }
alloy-eips = { version = "^2.0.0", default-features = false }
alloy-hardforks = { version = "^0.4.7" }
alloy-primitives = { version = "^1.0.0", default-features = false }
alloy-rpc-types-engine = { version = "^2.0.0", optional = true, default-features = false }
alloy-rpc-types-eth = { version = "^2.0.0", optional = true, default-features = false }
alloy-sol-types = { version = "^1.0.0", default-features = false }
auto_impl = { version = "^1" }
derive_more = { version = "^2", features = ["full"], default-features = false }
revm = { version = "^43.0.0", default-features = false }
thiserror = { version = "^2.0.0", default-features = false }
tracing = { version = "^0.1", default-features = false }

[dev-dependencies]
alloy-primitives = { version = "^1.0.0", features = ["serde"], default-features = false }
serde_json = { version = "^1" }
test-case = { version = "^3" }
