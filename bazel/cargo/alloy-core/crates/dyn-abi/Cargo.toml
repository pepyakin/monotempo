# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-core/crates/dyn-abi/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-dyn-abi"
version = "1.7.3"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_dyn_abi"
path = "src/lib.rs"

[[bench]]
name = "abi"
path = "benches/abi.rs"

[[bench]]
name = "types"
path = "benches/types.rs"

[features]
default = ["std"]
std = [
    "alloy-json-abi/std",
    "alloy-sol-types/std",
    "alloy-sol-type-parser/std",
    "alloy-primitives/std",
    "serde?/std",
    "serde_json?/std",
]
eip712 = [
    "alloy-sol-types/eip712-serde",
    "alloy-sol-type-parser/eip712",
    "dep:derive_more",
    "dep:serde",
    "dep:serde_json",
]
arbitrary = ["std", "alloy-sol-types/arbitrary", "dep:arbitrary", "dep:proptest"]
debug = ["std", "alloy-sol-type-parser/debug"]

[dependencies]
alloy-json-abi = { path = "../json-abi", default-features = false }
alloy-primitives = { path = "../primitives", default-features = false }
alloy-sol-type-parser = { path = "../sol-type-parser", default-features = false }
alloy-sol-types = { path = "../sol-types", default-features = false }
arbitrary = { version = "^1.3", optional = true }
derive_more = { version = "^2.0", features = ["deref", "deref_mut", "from", "into", "into_iterator"], optional = true, default-features = false }
itoa = { version = "^1" }
proptest = { version = "^1", optional = true, default-features = false }
serde = { version = "^1.0", features = ["alloc"], optional = true, default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], optional = true, default-features = false }
winnow = { version = "^1.0", features = ["alloc", "ascii"], default-features = false }

[dev-dependencies]
criterion = { version = "^0.7" }
ethabi = { version = "^18" }
rand = { version = "^0.9", features = ["os_rng", "thread_rng"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
