# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-core/crates/sol-types/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-sol-types"
version = "1.7.3"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_sol_types"
path = "src/lib.rs"

[[test]]
name = "compiletest"
path = "tests/compiletest.rs"

[[test]]
name = "derives"
path = "tests/derives.rs"

[[test]]
name = "doctests"
path = "tests/doctests.rs"

[[test]]
name = "macros"
path = "tests/macros/main.rs"

[features]
default = ["std"]
std = ["alloy-primitives/std", "alloy-json-abi?/std", "serde?/std"]
json = ["dep:alloy-json-abi", "alloy-sol-macro/json"]
eip712-serde = ["dep:serde", "alloy-primitives/serde"]
arbitrary = ["alloy-primitives/arbitrary"]
more-tuple-impls = []

[dependencies]
alloy-json-abi = { path = "../json-abi", optional = true, default-features = false }
alloy-primitives = { path = "../primitives", default-features = false }
alloy-sol-macro = { path = "../sol-macro", default-features = false }
serde = { version = "^1.0", features = ["alloc", "derive"], optional = true, default-features = false }

[dev-dependencies]
alloy-primitives = { path = "../primitives", features = ["arbitrary", "serde", "rlp"], default-features = false }
alloy-rlp = { version = "^0.3", features = ["core-net", "core-error"], default-features = false }
derive_more = { version = "^2.0", default-features = false }
paste = { version = "^1.0" }
pretty_assertions = { version = "^1.4" }
proptest = { version = "^1", default-features = false }
rustversion = { version = "^1.0" }
serde = { version = "^1.0", features = ["alloc", "derive"], default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
trybuild = { version = "^1.0" }
