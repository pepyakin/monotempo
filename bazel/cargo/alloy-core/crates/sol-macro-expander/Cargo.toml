# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-core/crates/sol-macro-expander/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-sol-macro-expander"
version = "1.7.3"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_sol_macro_expander"
path = "src/lib.rs"

[features]
json = ["dep:alloy-json-abi", "alloy-sol-macro-input/json"]

[dependencies]
alloy-json-abi = { path = "../json-abi", optional = true, default-features = false }
alloy-sol-macro-input = { path = "../sol-macro-input", default-features = false }
heck = { version = "^0.5" }
const-hex = { version = "^1.14", features = ["alloc", "core-error"], default-features = false }
indexmap = { version = "^2" }
proc-macro-error3 = { version = ">=3.0.0, <=3.0.1" }
proc-macro2 = { version = "^1.0" }
quote = { version = "^1.0" }
sha3 = { version = "^0.11.0", default-features = false }
syn = { version = "^2.0", features = ["extra-traits"] }
syn-solidity = { path = "../syn-solidity", features = ["visit", "visit-mut"], default-features = false }
