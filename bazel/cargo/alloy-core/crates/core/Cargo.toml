# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-core/crates/core/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-core"
version = "1.7.3"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_core"
path = "src/lib.rs"

[[test]]
name = "sol"
path = "tests/sol.rs"

[features]
default = [
    "std",
    "alloy-primitives/default",
    "alloy-dyn-abi?/default",
    "alloy-json-abi?/default",
    "alloy-sol-types?/default",
    "alloy-rlp?/default",
]
std = [
    "alloy-primitives/std",
    "alloy-json-abi?/std",
    "alloy-dyn-abi?/std",
    "alloy-sol-types?/std",
    "alloy-rlp?/std",
]
nightly = ["alloy-primitives/nightly"]
dyn-abi = ["sol-types", "dep:alloy-dyn-abi"]
json-abi = ["json", "serde", "dep:alloy-json-abi"]
json = ["alloy-sol-types?/json"]
sol-types = ["dep:alloy-sol-types"]
asm-keccak = ["alloy-primitives/asm-keccak"]
native-keccak = ["alloy-primitives/native-keccak"]
sha3-keccak = ["alloy-primitives/sha3-keccak"]
tiny-keccak = ["alloy-primitives/tiny-keccak"]
keccak-cache = ["alloy-primitives/keccak-cache"]
keccak-cache-global = ["alloy-primitives/keccak-cache-global"]
map = ["alloy-primitives/map"]
map-hashbrown = ["alloy-primitives/map-hashbrown"]
map-indexmap = ["alloy-primitives/map-indexmap"]
map-foldhash = ["alloy-primitives/map-foldhash"]
map-rapidhash = ["alloy-primitives/map-rapidhash"]
map-fxhash = ["alloy-primitives/map-fxhash"]
getrandom = ["alloy-primitives/getrandom"]
rand = ["alloy-primitives/rand"]
rayon = ["alloy-primitives/rayon"]
rkyv = ["alloy-primitives/rkyv"]
rlp = ["alloy-primitives/rlp", "dep:alloy-rlp"]
serde = ["alloy-primitives/serde"]
k256 = ["alloy-primitives/k256"]
secp256k1 = ["alloy-primitives/secp256k1"]
eip712 = ["alloy-sol-types?/eip712-serde", "alloy-dyn-abi?/eip712"]
more-tuple-impls = ["alloy-sol-types/more-tuple-impls"]
postgres = ["std", "alloy-primitives/postgres"]
arbitrary = ["std", "alloy-primitives/arbitrary", "alloy-sol-types?/arbitrary", "alloy-dyn-abi?/arbitrary"]

[dependencies]
alloy-dyn-abi = { path = "../dyn-abi", optional = true, default-features = false }
alloy-json-abi = { path = "../json-abi", optional = true, default-features = false }
alloy-primitives = { path = "../primitives", default-features = false }
alloy-rlp = { version = "^0.3", features = ["core-net", "core-error"], optional = true, default-features = false }
alloy-sol-types = { path = "../sol-types", optional = true, default-features = false }
