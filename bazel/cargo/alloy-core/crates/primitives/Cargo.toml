# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `alloy-core/crates/primitives/Cargo.toml`: the same
# package as `cargo metadata` describes it in the alloy-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "alloy-primitives"
version = "1.7.3"
edition = "2024"
rust-version = "1.85"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "alloy_primitives"
path = "src/lib.rs"

[[bench]]
name = "primitives"
path = "benches/primitives.rs"
required-features = ["rand"]

[features]
default = ["std", "map", "map-foldhash"]
std = [
    "bytes/std",
    "hex/std",
    "ruint/std",
    "alloy-rlp?/std",
    "foldhash?/std",
    "rapidhash?/std",
    "indexmap?/std",
    "k256?/std",
    "secp256k1?/std",
    "secp256k1?/global-context",
    "keccak-asm?/std",
    "proptest?/std",
    "rand?/std",
    "rand?/thread_rng",
    "rustc-hash?/std",
    "serde?/std",
    "fixed-cache?/std",
]
nightly = [
    "foldhash?/nightly",
    "rapidhash?/nightly",
    "hashbrown?/nightly",
    "hex/nightly",
    "fixed-cache?/nightly",
]
asm-keccak = ["dep:keccak-asm"]
native-keccak = []
sha3-keccak = []
tiny-keccak = ["dep:tiny-keccak"]
keccak-cache = ["std", "dep:rapidhash", "dep:fixed-cache"]
keccak-cache-global = ["keccak-cache"]
keccak-cache-stats = ["keccak-cache", "fixed-cache/stats"]
map = ["dep:hashbrown", "dep:foldhash"]
map-hashbrown = ["map"]
map-indexmap = ["map", "dep:indexmap"]
map-foldhash = ["map", "dep:foldhash"]
map-rapidhash = ["map", "dep:rapidhash"]
map-fxhash = ["map", "dep:rustc-hash"]
getrandom = ["dep:getrandom"]
k256 = ["dep:k256"]
secp256k1 = ["dep:secp256k1"]
rand = ["dep:rand", "getrandom", "ruint/rand-09", "rapidhash?/rand", "rustc-hash?/rand"]
rayon = ["dep:rayon", "hashbrown?/rayon", "indexmap?/rayon"]
rkyv = ["dep:rkyv", "ruint/rkyv"]
rlp = ["dep:alloy-rlp", "ruint/alloy-rlp"]
serde = [
    "dep:serde",
    "bytes/serde",
    "hex/serde",
    "ruint/serde",
    "hashbrown?/serde",
    "indexmap?/serde",
    "rand?/serde",
]
schemars = ["dep:schemars"]
borsh = ["dep:borsh", "ruint/borsh"]
allocative = ["dep:allocative"]
arbitrary = [
    "std",
    "dep:arbitrary",
    "dep:proptest",
    "dep:proptest-derive",
    "ruint/arbitrary",
    "ruint/proptest",
    "indexmap?/arbitrary",
]
postgres = ["std", "dep:postgres-types", "ruint/postgres"]
diesel = ["std", "dep:diesel", "ruint/diesel"]
sqlx = ["std", "dep:sqlx-core", "ruint/sqlx"]
sqlx-postgres = ["sqlx", "dep:sqlx-postgres"]
hex-compat = ["hex/hex"]

[dependencies]
allocative = { version = "^0.3.2", optional = true, default-features = false }
alloy-rlp = { version = "^0.3", features = ["core-net", "core-error"], optional = true, default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
borsh = { version = "^1.5", features = ["derive"], optional = true, default-features = false }
bytes = { version = "^1", default-features = false }
cfg-if = { version = "^1.0.0" }
derive_more = { version = "^2.0", features = ["as_ref", "add", "add_assign", "not", "deref", "deref_mut", "from", "from_str", "index", "index_mut", "into", "into_iterator", "display", "deref"], default-features = false }
diesel = { version = "^2.2", optional = true }
fixed-cache = { version = "^0.1.8", optional = true, default-features = false }
foldhash = { version = "^0.2", optional = true, default-features = false }
getrandom = { version = "^0.4", optional = true }
hashbrown = { version = "^0.17", features = ["default-hasher", "inline-more"], optional = true, default-features = false }
hex = { package = "const-hex", version = "^1.14", features = ["alloc", "core-error"], default-features = false }
indexmap = { version = "^2.5", optional = true, default-features = false }
itoa = { version = "^1" }
k256 = { version = "^0.13", features = ["ecdsa"], optional = true, default-features = false }
keccak-asm = { version = "^0.1.5", optional = true, default-features = false }
paste = { version = "^1.0" }
postgres-types = { version = "^0.2.6", optional = true }
proptest = { version = "^1", optional = true, default-features = false }
proptest-derive = { version = "^0.8", optional = true }
rand = { version = "^0.9", features = ["os_rng"], optional = true, default-features = false }
rapidhash = { version = "^4", optional = true, default-features = false }
rayon = { version = "^1.2", optional = true, default-features = false }
rkyv = { version = "^0.8", optional = true }
ruint = { version = "^1.16.0", features = ["alloc"], default-features = false }
rustc-hash = { version = "^2.1", optional = true, default-features = false }
schemars = { version = "^1", optional = true, default-features = false }
secp256k1 = { version = "^0.31", features = ["recovery", "alloc"], optional = true, default-features = false }
serde = { version = "^1.0", features = ["alloc", "derive"], optional = true, default-features = false }
sha3 = { version = "^0.11.0", default-features = false }
sqlx-core = { version = "^0.8", optional = true }
sqlx-postgres = { version = "^0.8", optional = true }
tiny-keccak = { version = "^2.0", features = ["keccak"], optional = true, default-features = false }

[dev-dependencies]
bcs = { version = "^0.2.1" }
bincode = { version = "=1.3.3" }
criterion = { version = "^0.7" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
