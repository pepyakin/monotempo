# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth-core/crates/zstd-compressors/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-zstd-compressors"
version = "0.7.1"
edition = "2024"
rust-version = "1.93"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_zstd_compressors"
path = "src/lib.rs"

[features]
default = ["std"]
std = []

[dependencies]
zstd = { version = "^0.13", features = ["experimental"] }
