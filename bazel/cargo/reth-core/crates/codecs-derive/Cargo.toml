# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth-core/crates/codecs-derive/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-codecs-derive"
version = "0.7.1"
edition = "2024"
rust-version = "1.93"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_codecs_derive"
path = "src/lib.rs"
proc-macro = true

[dependencies]
proc-macro2 = { version = "^1.0" }
quote = { version = "^1.0" }
syn = { version = "^2.0" }

[dev-dependencies]
similar-asserts = { version = "^1.5.0", features = ["serde"] }
syn = { version = "^2.0", features = ["full", "extra-traits"] }
