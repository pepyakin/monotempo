# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth-core/crates/codecs/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-codecs"
version = "0.7.1"
edition = "2024"
rust-version = "1.93"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_codecs"
path = "src/lib.rs"

[features]
default = ["std", "alloy"]
std = [
    "alloy-primitives/std",
    "bytes/std",
    "alloy-consensus?/std",
    "alloy-eips?/std",
    "alloy-genesis?/std",
    "alloy-trie?/std",
    "serde/std",
    "reth-zstd-compressors?/std",
    "dep:parity-scale-codec",
]
alloy = [
    "dep:alloy-consensus",
    "dep:alloy-eips",
    "dep:alloy-genesis",
    "dep:alloy-trie",
    "dep:reth-zstd-compressors",
]
test-utils = ["std", "alloy", "arbitrary", "dep:visibility", "dep:arbitrary"]
serde = [
    "alloy-consensus?/serde",
    "alloy-eips?/serde",
    "alloy-primitives/serde",
    "alloy-trie?/serde",
    "bytes/serde",
]
arbitrary = [
    "alloy-consensus?/arbitrary",
    "alloy-eips?/arbitrary",
    "alloy-primitives/arbitrary",
    "alloy-trie?/arbitrary",
]

[dependencies]
alloy-consensus = { version = "^2.0", optional = true, default-features = false }
alloy-eips = { version = "^2.0", features = ["serde"], optional = true, default-features = false }
alloy-genesis = { version = "^2.0", optional = true, default-features = false }
alloy-primitives = { version = "^1.5.6", features = ["map-foldhash"], default-features = false }
alloy-trie = { version = "^0.9.4", optional = true, default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
bytes = { version = "^1.11.1", default-features = false }
modular-bitfield = { version = "^0.13.1" }
parity-scale-codec = { version = "^3.2.1", features = ["bytes"], optional = true }
reth-codecs-derive = { path = "../codecs-derive" }
reth-zstd-compressors = { path = "../zstd-compressors", optional = true, default-features = false }
serde = { version = "^1.0", default-features = false }
visibility = { version = "^0.1.1", optional = true }

[dev-dependencies]
alloy-consensus = { version = "^2.0", features = ["arbitrary"], default-features = false }
alloy-eips = { version = "^2.0", features = ["arbitrary", "serde"], default-features = false }
alloy-primitives = { version = "^1.5.6", features = ["map-foldhash", "arbitrary", "serde", "rand"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
proptest = { version = "^1.6" }
proptest-arbitrary-interop = { version = "^0.1" }
rstest = { version = "^0.26.1" }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
test-fuzz = { version = "^7" }
