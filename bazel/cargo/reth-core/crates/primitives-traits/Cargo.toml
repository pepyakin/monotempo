# GENERATED FILE - DO NOT EDIT. Regenerate with `python3 bazel/generate.py`.
#
# Bazel-only derivation of `reth-core/crates/primitives-traits/Cargo.toml`: the same
# package as `cargo metadata` describes it in the reth-core workspace, with
# workspace inheritance resolved. Cargo builds are unaffected.

[package]
name = "reth-primitives-traits"
version = "0.7.1"
edition = "2024"
rust-version = "1.93"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false

[lib]
name = "reth_primitives_traits"
path = "src/lib.rs"

[features]
default = ["std", "quanta"]
std = [
    "alloy-consensus/std",
    "alloy-eip7928?/std",
    "alloy-eips/std",
    "alloy-genesis/std",
    "alloy-primitives/std",
    "revm-primitives/std",
    "serde?/std",
    "alloy-rlp/std",
    "bytes/std",
    "derive_more/std",
    "once_cell/std",
    "secp256k1?/std",
    "thiserror/std",
    "alloy-trie/std",
    "serde_json/std",
    "revm-bytecode/std",
    "revm-state/std",
    "alloy-rpc-types-eth?/std",
]
quanta = ["std", "dep:quanta"]
secp256k1 = ["alloy-consensus/secp256k1"]
test-utils = ["arbitrary", "reth-codecs?/test-utils"]
arbitrary = [
    "std",
    "alloy-consensus/arbitrary",
    "alloy-eip7928?/arbitrary",
    "alloy-primitives/arbitrary",
    "dep:arbitrary",
    "dep:proptest",
    "dep:proptest-arbitrary-interop",
    "alloy-eips/arbitrary",
    "revm-primitives/arbitrary",
    "reth-codecs?/arbitrary",
    "secp256k1?/global-context",
    "secp256k1?/rand",
    "alloy-trie/arbitrary",
    "alloy-rpc-types-eth?/arbitrary",
    "dashmap?/arbitrary",
]
serde = [
    "dep:serde",
    "alloy-consensus/serde",
    "alloy-eip7928?/serde",
    "alloy-eips/serde",
    "alloy-primitives/serde",
    "bytes/serde",
    "rand/serde",
    "reth-codecs?/serde",
    "revm-primitives/serde",
    "revm-primitives/serde",
    "secp256k1?/serde",
    "alloy-trie/serde",
    "revm-bytecode/serde",
    "revm-state/serde",
    "rand_08/serde",
    "alloy-rpc-types-eth?/serde",
    "dashmap?/serde",
]
reth-codec = ["dep:reth-codecs", "dep:modular-bitfield", "dep:byteorder"]
rayon = ["dep:rayon"]
dashmap = ["dep:dashmap"]
rpc-compat = ["alloy-rpc-types-eth"]
eip7928 = ["dep:alloy-eip7928"]

[dependencies]
alloy-consensus = { version = "^2.0", features = ["k256"], default-features = false }
alloy-eip7928 = { version = "^0.4.10", optional = true, default-features = false }
alloy-eips = { version = "^2.0", default-features = false }
alloy-genesis = { version = "^2.0", default-features = false }
alloy-primitives = { version = "^1.5.6", features = ["map-foldhash", "k256"], default-features = false }
alloy-rlp = { version = "^0.3.13", features = ["core-net"], default-features = false }
alloy-rpc-types-eth = { version = "^2.0", optional = true, default-features = false }
alloy-trie = { version = "^0.9.4", default-features = false }
arbitrary = { version = "^1.3", features = ["derive"], optional = true }
byteorder = { version = "^1", optional = true }
bytes = { version = "^1.11.1", default-features = false }
dashmap = { version = "^6.0", features = ["inline"], optional = true }
derive_more = { version = "^2", features = ["full"], default-features = false }
modular-bitfield = { version = "^0.13.1", optional = true }
once_cell = { version = "^1.19", features = ["critical-section"], default-features = false }
proptest = { version = "^1.6", optional = true }
proptest-arbitrary-interop = { version = "^0.1", optional = true }
quanta = { version = "^0.12", optional = true }
rayon = { version = "^1.7", optional = true }
reth-codecs = { path = "../codecs", optional = true, default-features = false }
revm-bytecode = { version = "^43.0.0", default-features = false }
revm-primitives = { version = "^43.0.0", default-features = false }
revm-state = { version = "^43.0.0", default-features = false }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "recovery"], optional = true, default-features = false }
serde = { version = "^1.0", optional = true, default-features = false }
thiserror = { version = "^2.0.0", default-features = false }

[dev-dependencies]
alloy-consensus = { version = "^2.0", features = ["arbitrary", "serde"], default-features = false }
alloy-primitives = { version = "^1.5.6", features = ["map-foldhash", "arbitrary", "serde"], default-features = false }
arbitrary = { version = "^1.3", features = ["derive"] }
byteorder = { version = "^1" }
modular-bitfield = { version = "^0.13.1" }
proptest = { version = "^1.6" }
proptest-arbitrary-interop = { version = "^0.1" }
rand = { version = "^0.9" }
rand_08 = { package = "rand", version = "^0.8" }
reth-codecs = { path = "../codecs", default-features = false }
secp256k1 = { version = "^0.30", features = ["global-context", "recovery", "recovery", "global-context", "rand"], default-features = false }
serde = { version = "^1.0", default-features = false }
serde_json = { version = "^1.0", features = ["alloc"], default-features = false }
