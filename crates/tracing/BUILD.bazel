# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 scripts/bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "reth_cargo_toml_env_vars", "reth_library", "reth_unit_test")

FEATURES = [
    "clap",
    "default",
    "eyre",
    "otlp",
    "otlp-logs",
    "reth-tracing-otlp",
    "rolling-file",
    "std",
    "tracing-appender",
    "tracing-chrome",
    "tracing-journald",
    "tracing-logfmt",
    "tracing-samply",
    "tracing-subscriber",
]

DECLARED_FEATURES = [
    "clap",
    "default",
    "eyre",
    "otlp",
    "otlp-logs",
    "reth-tracing-otlp",
    "rolling-file",
    "std",
    "tracing-appender",
    "tracing-chrome",
    "tracing-journald",
    "tracing-logfmt",
    "tracing-samply",
    "tracing-subscriber",
    "tracing-tracy",
    "tracy",
    "tracy-client",
]

reth_cargo_toml_env_vars()

reth_library(
    name = "reth_tracing",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//crates/tracing-otlp:reth_tracing_otlp",
        "@crates//:clap",
        "@crates//:eyre",
        "@crates//:rolling-file",
        "@crates//:tracing",
        "@crates//:tracing-appender",
        "@crates//:tracing-chrome",
        "@crates//:tracing-journald",
        "@crates//:tracing-logfmt",
        "@crates//:tracing-samply",
        "@crates//:tracing-subscriber",
    ],
)

reth_unit_test(
    name = "reth_tracing_test",
    crate = ":reth_tracing",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)
