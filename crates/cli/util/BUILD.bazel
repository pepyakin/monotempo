# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 scripts/bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "reth_cargo_toml_env_vars", "reth_library", "reth_unit_test")

FEATURES = [
    "jemalloc",
]

DECLARED_FEATURES = [
    "jemalloc",
    "jemalloc-prof",
    "snmalloc",
    "snmalloc-native",
    "tracy-allocator",
]

reth_cargo_toml_env_vars()

reth_library(
    name = "reth_cli_util",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//crates/fs-util:reth_fs_util",
        "@crates//:alloy-eips",
        "@crates//:alloy-primitives",
        "@crates//:cfg-if",
        "@crates//:eyre",
        "@crates//:rand_08-0.8.6",
        "@crates//:secp256k1",
        "@crates//:serde",
        "@crates//:thiserror",
    ] + select({
        "@platforms//os:linux": ["@crates//:libc", "@crates//:tikv-jemalloc-sys", "@crates//:tikv-jemallocator"],
        "@platforms//os:macos": ["@crates//:libc", "@crates//:tikv-jemalloc-sys", "@crates//:tikv-jemallocator"],
        "//conditions:default": [],
    }),
    aliases = {
        "@crates//:rand_08-0.8.6": "rand_08",
    },
)

reth_unit_test(
    name = "reth_cli_util_test",
    crate = ":reth_cli_util",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:rand-0.9.4",
    ],
)
