# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_integration_test", "crate_library", "crate_unit_test")
load("//alloy/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_signer_tempo",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy/crates/signer-local:alloy_signer_local",
        "@crates//:dirs",
        "@crates//:serde",
        "@crates//:thiserror",
        "@crates//:toml-0.8.23",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "alloy_signer_tempo_test",
    workspace = WORKSPACE,
    crate = ":alloy_signer_tempo",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:tempfile",
    ],
    process_per_test = True,
)

crate_integration_test(
    name = "alloy_signer_tempo_env_test",
    workspace = WORKSPACE,
    crate_root = "tests/env.rs",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy/crates/signer-local:alloy_signer_local",
        ":alloy_signer_tempo",
        "@crates//:dirs",
        "@crates//:serde",
        "@crates//:tempfile",
        "@crates//:thiserror",
        "@crates//:toml-0.8.23",
        "@crates//:tracing",
    ],
    process_per_test = True,
)

crate_integration_test(
    name = "alloy_signer_tempo_load_test",
    workspace = WORKSPACE,
    crate_root = "tests/load.rs",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy/crates/signer-local:alloy_signer_local",
        ":alloy_signer_tempo",
        "@crates//:dirs",
        "@crates//:serde",
        "@crates//:tempfile",
        "@crates//:thiserror",
        "@crates//:toml-0.8.23",
        "@crates//:tracing",
    ],
    process_per_test = True,
)

crate_integration_test(
    name = "alloy_signer_tempo_lookup_test",
    workspace = WORKSPACE,
    crate_root = "tests/lookup.rs",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy/crates/signer-local:alloy_signer_local",
        ":alloy_signer_tempo",
        "@crates//:dirs",
        "@crates//:serde",
        "@crates//:tempfile",
        "@crates//:thiserror",
        "@crates//:toml-0.8.23",
        "@crates//:tracing",
    ],
    process_per_test = True,
)
