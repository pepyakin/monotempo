# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//alloy/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "mnemonic",
    "mnemonic-all-languages",
    "zeroize",
]

DECLARED_FEATURES = [
    "eip712",
    "keystore",
    "keystore-geth-compat",
    "mnemonic",
    "mnemonic-all-languages",
    "secp256k1",
    "yubihsm",
    "zeroize",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_signer_local",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy/crates/consensus:alloy_consensus",
        "//alloy/crates/network:alloy_network",
        "//alloy/crates/signer:alloy_signer",
        "@crates//:coins-bip32",
        "@crates//:coins-bip39",
        "@crates//:k256",
        "@crates//:rand-0.8.6",
        "@crates//:thiserror",
        "@crates//:zeroize",
    ],
    proc_macro_deps = [
        "@crates//:async-trait",
    ],
)

crate_unit_test(
    name = "alloy_signer_local_test",
    workspace = WORKSPACE,
    crate = ":alloy_signer_local",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/dyn-abi:alloy_dyn_abi",
        "//alloy-core/crates/sol-types:alloy_sol_types",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tempfile",
        "@crates//:tokio",
        "@crates//:yubihsm",
    ],
)
