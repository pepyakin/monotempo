# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//alloy/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = [
    "throttle",
    "wasm-bindgen",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_transport",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/json-rpc:alloy_json_rpc",
        "@crates//:base64",
        "@crates//:derive_more",
        "@crates//:futures",
        "@crates//:futures-utils-wasm",
        "@crates//:parking_lot",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:thiserror",
        "@crates//:tokio",
        "@crates//:tower",
        "@crates//:tracing",
        "@crates//:url",
    ],
    proc_macro_deps = [
        "@crates//:auto_impl",
    ],
)

crate_unit_test(
    name = "alloy_transport_test",
    workspace = WORKSPACE,
    crate = ":alloy_transport",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:alloy-primitives",
    ],
)
