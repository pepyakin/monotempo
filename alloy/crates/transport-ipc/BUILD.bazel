# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_integration_test", "crate_library", "crate_unit_test")
load("//alloy/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "mock",
]

DECLARED_FEATURES = [
    "default",
    "mock",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_transport_ipc",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/json-rpc:alloy_json_rpc",
        "//alloy/crates/pubsub:alloy_pubsub",
        "//alloy/crates/transport:alloy_transport",
        "@crates//:bytes",
        "@crates//:futures",
        "@crates//:interprocess",
        "@crates//:memchr",
        "@crates//:pin-project",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tempfile",
        "@crates//:tokio",
        "@crates//:tokio-util",
        "@crates//:tracing",
    ],
)

crate_unit_test(
    name = "alloy_transport_ipc_test",
    workspace = WORKSPACE,
    crate = ":alloy_transport_ipc",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:tokio-test",
    ],
)

crate_integration_test(
    name = "alloy_transport_ipc_ipc_test",
    workspace = WORKSPACE,
    crate_root = "tests/ipc.rs",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/json-rpc:alloy_json_rpc",
        "//alloy/crates/pubsub:alloy_pubsub",
        "//alloy/crates/transport:alloy_transport",
        ":alloy_transport_ipc",
        "@crates//:bytes",
        "@crates//:futures",
        "@crates//:interprocess",
        "@crates//:memchr",
        "@crates//:pin-project",
        "@crates//:serde",
        "@crates//:serde_json",
        "@crates//:tempfile",
        "@crates//:tokio",
        "@crates//:tokio-test",
        "@crates//:tokio-util",
        "@crates//:tracing",
    ],
)
