# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//alloy/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "std",
]

DECLARED_FEATURES = [
    "borsh",
    "default",
    "serde-bincode-compat",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "alloy_genesis",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/eips:alloy_eips",
        "//alloy/crates/serde:alloy_serde",
        "@crates//:alloy-primitives",
        "@crates//:alloy-trie",
        "@crates//:serde",
    ],
    compile_data = [
        "dumpgenesis/holesky.json",
        "dumpgenesis/mainnet.json",
        "dumpgenesis/sepolia.json",
    ],
)

crate_unit_test(
    name = "alloy_genesis_test",
    workspace = WORKSPACE,
    crate = ":alloy_genesis",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@crates//:bincode-2.0.1",
        "@crates//:serde_json",
    ],
)
