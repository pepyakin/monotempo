# Workspace exports and node entry point; crate targets are generated separately.

package(default_visibility = ["//visibility:public"])

exports_files(["Cargo.toml", "Cargo.lock", ".github/workflows/bench.yml"])

alias(
    name = "tempo",
    actual = "//tempo/bin/tempo:tempo",
)
