# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 tempo/scripts/bazel/generate.py` after changing Cargo.toml.

load("//tempo/bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//tempo/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "evm",
    "serde",
    "std",
]

DECLARED_FEATURES = [
    "default",
    "evm",
    "serde",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "tempo_hardfork",
    workspace = WORKSPACE,
    version = "1.11.0",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@tempo_crates//:alloy-eips",
        "@tempo_crates//:alloy-evm",
        "@tempo_crates//:alloy-hardforks",
        "@tempo_crates//:serde",
    ],
    proc_macro_deps = [
        "@tempo_crates//:paste",
    ],
)

crate_unit_test(
    name = "tempo_hardfork_test",
    workspace = WORKSPACE,
    version = "1.11.0",
    crate = ":tempo_hardfork",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@tempo_crates//:serde_json",
    ],
)

exports_files(["Cargo.toml"])
