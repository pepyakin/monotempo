# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 tempo/scripts/bazel/generate.py` after changing Cargo.toml.

load("//tempo/bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//tempo/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "tempo_consensus_config",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@tempo_crates//:age",
        "@tempo_crates//:commonware-codec",
        "@tempo_crates//:commonware-cryptography",
        "@tempo_crates//:commonware-math",
        "@tempo_crates//:const-hex",
        "@tempo_crates//:rand_core",
        "@tempo_crates//:secrecy",
        "@tempo_crates//:thiserror",
    ],
)

crate_unit_test(
    name = "tempo_consensus_config_test",
    workspace = WORKSPACE,
    crate = ":tempo_consensus_config",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "@tempo_crates//:commonware-utils",
        "@tempo_crates//:rand-0.10.2",
        "@tempo_crates//:tempfile",
    ],
)

exports_files(["Cargo.toml"])
