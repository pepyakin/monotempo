# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//tempo/bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//tempo/bazel:workspace.bzl", "WORKSPACE")

FEATURES = [
    "default",
    "rpc",
    "serde",
    "std",
]

DECLARED_FEATURES = [
    "default",
    "rpc",
    "serde",
    "std",
]

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "tempo_contracts",
    workspace = WORKSPACE,
    version = "1.11.0",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "//alloy-core/crates/sol-types:alloy_sol_types",
        "//alloy/crates/contract:alloy_contract",
        "//tempo/crates/hardfork:tempo_hardfork",
        "@crates//:serde",
    ],
)

crate_unit_test(
    name = "tempo_contracts_test",
    workspace = WORKSPACE,
    version = "1.11.0",
    crate = ":tempo_contracts",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/provider:alloy_provider",
        "@crates//:tokio",
    ],
)
