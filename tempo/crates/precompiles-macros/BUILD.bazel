# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 bazel/generate.py` after changing Cargo.toml.

load("//tempo/bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_proc_macro", "crate_unit_test")
load("//tempo/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_proc_macro(
    name = "tempo_precompiles_macros",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy-core/crates/primitives:alloy_primitives",
        "@crates//:proc-macro2",
        "@crates//:quote",
        "@crates//:syn",
    ],
)

crate_unit_test(
    name = "tempo_precompiles_macros_test",
    workspace = WORKSPACE,
    crate = ":tempo_precompiles_macros",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//alloy/crates/alloy:alloy",
    ],
)
