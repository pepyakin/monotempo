# GENERATED FILE - DO NOT EDIT.
#
# Regenerate with `python3 tempo/scripts/bazel/generate.py` after changing Cargo.toml.

load("//tempo/bazel:rust.bzl", "crate_cargo_toml_env_vars", "crate_library", "crate_unit_test")
load("//tempo/bazel:workspace.bzl", "WORKSPACE")

FEATURES = []

DECLARED_FEATURES = []

crate_cargo_toml_env_vars(WORKSPACE)

crate_library(
    name = "tempo_faucet",
    workspace = WORKSPACE,
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
    deps = [
        "//tempo/crates/alloy:tempo_alloy",
        "//tempo/crates/precompiles:tempo_precompiles",
        "@tempo_crates//:alloy",
        "@tempo_crates//:clap",
        "@tempo_crates//:jsonrpsee",
        "@tempo_crates//:reth-rpc-server-types",
    ],
    proc_macro_deps = [
        "@tempo_crates//:async-trait",
    ],
)

crate_unit_test(
    name = "tempo_faucet_test",
    workspace = WORKSPACE,
    crate = ":tempo_faucet",
    crate_features = FEATURES,
    declared_features = DECLARED_FEATURES,
)

exports_files(["Cargo.toml"])
